# 下一批开发需求：总表与交付计划

日期：2026-09-11。源码审计基线：`0bb96707d78af10c2c4f6ed8e6117849c26da970`。

状态：**Q1 95 项、Q2 9 项均已回答并归并；主题执行计划已建立，产品实现未开始** 。

归并日期：2026-09-14。Q1 原答归档为 `3619dee`；本轮已先提交并推送 `7227090`，保留 Q1 归并文档及 Q2 全部原答。其后的 Q2 整理改动留工作区，未再次提交。

需求来源：本次用户上传的 `pasted-text.txt`（附件标识 `3f608f5a-e204-4910-8307-61c8654e2092`）。下列 R01–R32 与附件中 32 个一级条目顺序一致，不沿用上一批开发的阶段编号。

## 1. 阅读入口与事实边界

| 文档 | 用途 |
|---|---|
| 本文 | 完整需求索引、原优先级、依赖、建议开发批次和共同验收门 |
| [01：交互、显示与常规 UI](01-Interaction-and-Display.md) | 已有问题的源码线索、各视图覆盖面、修改边界和测试项 |
| [02：乐器变化点、Lane Tabs 与事件呈现](02-Instrument-Changes-and-Lanes.md) | 最高优先级音色选择补漏，以及与 Catalog、原始 MIDI、试听、事件列表的关系 |
| [03：多实例与工作区恢复](03-Multi-Instance-and-Workspace-State.md) | 两个独立大型专题、剪贴板协议、共享存储、视图状态所有权、文件打开 |
| [04：完整决策与文档问答清单](04-Decisions-and-Preparation.md) | 唯一问答入口：保留 Q1／Q2 原问题／推荐／原答；§13 九项 Q2 已回答，§14 为本轮收尾与进入条件 |
| [05：Q1／Q2 决策汇总](05-Decision-Summary-and-Q2-2026-09-14.md) | 104项决定导航、重要修订、Lane计数／琴键染色等技术答复及后续工程门；不是第二份答卷 |
| [execution：主题执行计划与验收总索引](execution/00-Execution-Plan.md) | 9份主题文档；近期A1/A2a/A2b/A3细分工程任务与人工批次，后续主题记录边界／依赖／验证门 |

本文档集使用以下术语：

- **用户要求** ：本次明确提出的目标；其内部仍未确定的选择继续列为待决策。
- **源码确认** ：在上述基线中读到的结构或执行路径，不等于已运行复现。
- **候选原因** ：能够解释报告现象，但本次未用运行探针或 UI 操作证明。
- **建议** ：用于后续批准的方案，不能据此擅自改变声音、文件格式或工作流。
- **待准备 / 待评估** ：分别表示需用户提供资料、或先做性能实验再决定正式实现。

初次归档及本轮决策复核均没有运行产品构建、性能测试、音频测试或 UI 验收，不把静态审计称为修复或测试通过。本目录是后续任务的输入，已决定的改动仍须在实施阶段正式更新相应规格。本轮按用户要求先提交／推送原文档和全部回答，再仅整理本目录 Markdown；不改产品代码、SRS、不变量、ADR、版本、用户草稿或发布物，后续整理不再自动提交／推送。

### 1.1 后续统一在文档问答

- Q1 的95项和追加 Q2 的9项都已有回答，无漏答；原32项无整项遗漏。本轮未发现必须追加的 Q3，优先级和各项待准备／性能条件保持不变。
- 用户可逐项填写“用户回答”，或在统一答复区写“除指定例外外，Q1 全部推荐”；未填写不算同意，具体子项答复优先。图标选择不包含在统一批准中。
- 后续追问、用户原答与结论持续记录在04；聊天只提示需查看／回答的编号。已回答的推荐发生实质变化时须保留记录并重新确认，不能用旧的“全部推荐”自动批准新问题。
- 01～03同步用户已确认修订，保留源码审计和测试细节；05只做归纳，详细问答仍只在04。Q1／Q2原推荐保留为历史，遇到不同口径以用户后续明确修订和归并状态为准；不能把本轮收敛当作对未来新问题的批准。正式SRS变更在对应实施切片落实。

## 2. 32 项需求总表

优先级严格按原文：P0=最高，P1=高，P2=中，P3=低，P4=最低。共 **P0 7 项、P1 7 项、P2 9 项、P3 8 项、P4 1 项** 。成本不改变优先级；大型 P0 不能因为耗时较长被默默降级。

| ID | 优先级 | 要求摘要 | 当前分类 / 主要依赖 | 任务文档 |
|---|---|---|---|---|
| R01 | P2 | 多实例；跨实例、跨项目共享剪贴板，重点支持 Note Art | 独立多阶段专题；写入协调、生命周期、对象范围与跨 TPQN 决策 | 03 |
| R02 | P4 | 弹窗、菜单、下拉动画 / 亚克力；瓦片加载及完成动效 | 正式发布且功能稳定后；动画示例不视为定案 | 01 |
| R03 | P3 | Windows 原生窗口动画、Win11 圆角阴影 / Win10 方角阴影 | 值得发布前单做；先验证与自绘 WindowChrome 兼容 | 01 |
| R04 | P3 | 拖入 `.midora` / MIDI 打开；支持 Windows Open With | 复用统一打开流程；与多实例目标窗口策略联动 | 03 |
| R05 | P1 | 底部 SoundFont 状态可点击，直接打开 SoundFonts 设置页 | 可先做；保留播放 / 前台任务与试听所有权规则 | 01 |
| R06 | P2 | 项目保存完整可恢复视图、Tabs / 顺序、Track Mute/Solo | 独立多阶段专题；先固定白名单，后版本化 presentation | 03 |
| R07 | P2 | 同 Track 的 Segment 通用视图参数即时共享、关闭仍记忆 | 为 R06 建立状态所有权；R28 的 lane 状态先定结构 | 03 |
| R08 | P3 | 大部分右键 / 顶部菜单增加图标 | **等待用户逐项选图标** ；独立全局检查，不进入04本次问卷 | 01 |
| R09 | P3 | 增加和重新设计快捷键 | 已授权给新增及以前无快捷键的命令补键；保留既有键位，不新增可配置改键系统 | 01、04 |
| R10 | P3 | 两类 Segment / All Tracks / Arrangement / Conductor 连续滚屏；全局Follow三态，锚点0～100%默认0% | **先测性能** ；Q2扩大范围；其他已有Follow视图未实现连续前沿用跳跃，不新增SubVoice跟随 | 01 |
| R11 | P3 | 两类 Segment / All Tracks 播放时左侧琴键染色，包含洋葱皮来源色 | Gate语义及颜色优先级已定；**先测性能** ，不用图像像素替代正式状态 | 01、04 |
| R12 | P2 | CC10、71～78显示−64..63，编辑工具表达式使用显示值，无raw提示 | 仅外侧编辑／工具显示域；Project Mapping和raw音乐语义不变，Help明确区别 | 02 |
| R13 | P1 | 三类事件 / 参数批移到 value 范围外 Clamp，不报数据字节越界 | Direct MIDI 有明确可疑标量编码路径；其他两类共同回归 | 01 |
| R14 | P0 | SubVoice 模板外创建事件自动扩模板，覆盖所有创建交互 | 下层已有扩展能力；UI 提前过滤是强候选原因 | 01 |
| R15 | P2 | SubVoice 模板末端手柄；Snap 作用于 delta；长度 / delta 提示 | 复用模板合法下界；不附带隐藏模板内容新语义 | 01、04 |
| R16 | P0 | Catalog Banks / Programs、Scan Presets 列表滚轮过量 | 逻辑项 offset 与通用像素滚动路由不匹配的候选原因 | 01 |
| R17 | P0 | 全局禁止已展开下拉列表底部悬停自动滚动 | 需通用 / 特化控件清单；不影响滚轮、键盘、拖柄 | 01 |
| R18 | P1 | 取消右双击；右拖固定框选，右单击抬起无故意延迟菜单 | Q2修订为保留共享Draw；事件区增加Free／Line／Horizontal形态组，非Draw禁用 | 01、04 |
| R19 | P1 | 可移动编辑指针但保留选择，同时保留左单击清选能力 | 已定Ctrl+左单击顶部时间尺；普通时间尺单击仍为播放指针 | 01、04 |
| R20 | P2 | 浮动工具新增13个常驻命令及指定图标；跨编辑模式显示 | 与R18联动；完整保留既有混合选择／有界编辑／类型能力边界 | 01 |
| R21 | P1 | 浮动 Move 垂直拖出钢琴卷帘仍继续响应 | viewport 命中 Clamp 是候选原因；与捕获 / 自动滚动一起查 | 01 |
| R22 | P3 | 所有标尺低缩放跳标以全局 0 为锚，平移不跳相位 | Bar label 以可见起点贪心取样；普通 Tick ruler 已部分正确 | 01 |
| R23 | P0 | New Project：Create 短文案，Browse 与窗口加宽防裁切 | 可先做；保留统一按钮 / 字体 / DPI 规则 | 01 |
| R24 | P0 | SubVoice Add Event 成功后切到新 Lane | 当前成功路径未明确切 target；需恢复视图焦点 | 01 |
| R25 | P0 | 三类钢琴卷帘对象 List 默认宽 400px | 当前共享默认 350；不覆盖用户后续主动调整 | 01 |
| R26 | P1 | List 的 value 列去无用 `0 ·` 等编号 | 按事件类型格式化，不删除有意义的 target 身份 | 01 |
| R27 | P0 | Bank / Program 名称选择、显式Instrument Changes包装点、500ms试听及Initial State统一选择器 | **核心独立切片** ；Q1已定，不从MIDI导入生成／投影包装 | 02、04 |
| R28 | P1 | 三类Lanes每目标一Tab；Vel./适用Inst.常驻；重排、溢出菜单、独立纵轴 | 应在R06前；普通Tab可隐藏，全部target目录及增量精确计数已确认 | 02 |
| R29 | P2 | 三类事件视图增加水平保持 / 垂直跳变线 | 复用 Tempo 的绘制思路，不能把所有 MIDI 消息当状态 | 02 |
| R30 | P3 | 两类 Segment 新 Tracks 左栏，复用 Arrangement 轨头操作 | 不建立第二套顺序 / 选择 / 分组业务；未来纳入 R06 | 01 |
| R31 | P2 | 编译时 Compile 按钮文字右侧显示百分比 | 当前只有阶段状态；需可靠且低开销的进度来源 | 01、04 |
| R32 | P2 | All Tracks初开自动选择≥3px/key且最接近显示全128键的缩放 | 已修订下限；首次有效布局计算，小窗口保留下限并允许垂直滚动 | 01、04 |

## 3. 需求追踪与不扩大范围

| 维度 | 本批约束 |
|---|---|
| 输入 | 当前 Project 正式数据、版本化编辑 / 选择、Catalog 数值名称、用户动作与可恢复视图字段 |
| 正式输出 | 原子 Project command 或独立 presentation / application 更新；显示投影不反向成为编译事实源 |
| 时间 / 身份 | 保持 `[start,end)`、稳定 ID、正式同 Tick 顺序、极端 Tick 安全算术；ordinal 仅在冻结修订内使用 |
| 碰撞 | Note exact start+key later-loses；数值事件 exact tick+target later-wins；导入未被编辑命中的重复不得被全局清洗 |
| 失败 | 取消、旧修订、资源不足、非法目标、编解码失败不得部分发布；旧项目 / 原数据仍可用 |
| 诊断 | 保留既有完整计数、分页和 README 截断策略；新增 UI 名称缺失只数值回退，不变音乐 Error |
| 持久化 | R06/07按批准白名单进入独立presentation；R01同目录持久文件型单个当前剪贴板槽，独立协议兼容／升级策略已定，工程版本与预算待实施验证；R27为正式稳定编辑分组，不私塞旧schema |
| 运行期 | 悬停、捕获、任务、预览、缓存、普通 Selection 不因“完整恢复”自动持久化；Mute/Solo 仍是监听过滤 |
| 非目标 | 不主动改音频合成 / 资源分配 / 编译声音语义，不恢复自动保存 / 崩溃快照，不实施菜单图标选型，不新增普通实时 MIDI OUT |

R27 的音色试听必须满足现有预览安全约束。若现有裸音高试听不能直接满足，应以窄范围正式试听适配解决，不以本批需求为名重写整个音频后端。

## 4. 规格变更清单：先记录，实施前再正式更新

下列是待调整位置，而不是宣称本次已修改 SRS。

| 新要求 | 现有约束 / 残留 | 实施前动作 |
|---|---|---|
| R01 | §3.3/3.14/21.2、§20.6、INV-019/086/094：单实例 / 同会话剪贴板 / 单实例回收；§16.17.2：普通保存不长锁且可覆盖外部改动 | 多实例、传输、同文件写入与共享存储 ADR；明确对象与 TPQN 支持范围 |
| R06/07 | §3.11、§16.28/16.33、§17.2/17.3.7、§19.3.3、§24.9，INV-011/060/091/112/116 | 扩展 presentation 白名单 / 独立 schema；将“Mute/Solo 不保存”改成“可保存运行期初值，不改变音乐输出” |
| R18/20 | §20.7.9.1、INV-097 固定 300ms 右双击；INV-098 浮动工具 Select 专属 | 一次替换完整手势表，迁移直线绘制入口并同步自动测试 |
| R27 | §8.55.1、§18.4.2 仍残留“不显示 GM/SF2 名称”，与更新的 §6.10/INV-101 不一致 | 清理旧措辞；不要将旧纯数值测试当作新需求不可做的依据 |
| R27/28 | §8.50.2/§18.4.5 的派生空 Lane，和 §9.1.6/§9.10.1 的可独立存在 Mapping owner | 区分隐藏 Tab、删除数据、删除 owner，禁止以关 Tab 偷偷删 Mapping |
| R12/27/29 | 原始值、Bank/Program独立／optional字段、Value Curve与Step参数语义 | 明确指定CC显示与工具表达式值域，raw编码不变；Mapping保持原raw音乐语义，Help区分两层，不默改声音 |

现行 Format 3 已独立分派 presentation schema（读 1/2、写 2）。扩展视图字段**不必然** 升级外层 Project Format；若 R27 引入旧 source 无法表达的正式编辑身份 / 对象，才需另外评估音乐 source 格式。不得提前承诺最终版本号。

## 5. 建议交付顺序

以下是排期建议，不代表用户已要求立即开发，也不是必须一口气完成全部批次。

2026-09-14执行规划补充：按用户要求建立 [独立执行目录](execution/00-Execution-Plan.md)，各主题在自己的文档里维护T-ID子任务、完成证据和UAT-ID人工清单。下面保留总体路线；具体执行与验收拆分查新目录。近期细化为36个内部任务，合并4轮人工验收、37个主检查场景；不把每个工程任务变成一次用户验收。后续A4细分为A4a事件显示与A4b模板／标尺／反馈，B/C等先保留轮廓，不预定全部最终轮数。

### A：近期交互与核心编辑主线

| 批次 | 内容 | 进入条件 / 验收主题 |
|---|---|---|
| A1 | R14/16/17/23/24/25，加关联小项 R05/13/26 | 优先解决最高优先级可独立项；同轮可分提交，列表 / 下拉 / 创建 / 批移分别验收。R13沿用现有共同delta饱和，不需重复定案 |
| A2a | R27包装与试听；统一选择器、Initial State、单点创建／Properties | P0决定已收齐；500ms试听、只显式包装；不等待中优先级完整状态专题 |
| A2b | R27全编辑／List／大规模包装显示；R28每目标Tabs | raw／wrapper一致性，导入Inst.为空；落实已确认目录／显隐和精确摘要；作为后续状态恢复基础 |
| A3 | R18/19/20/21统一鼠标状态机与浮动工具 | Q2已定共享Draw＋绘线形态组；一次验收覆盖Arrangement、三种Piano、Velocity、Event、Tempo |
| A4 | R12/15/29/31/32；可夹带独立低成本R22 | Q2已定工具显示域／Mapping不变及Help；模板下界／阶梯／诚实进度／≥3px Fit已定，互不依赖可拆 |

Q1／Q2已收敛，当前无新增待答问卷阻塞上述切片；仍需用户明确要求开始实施，再按P0/P1优先顺序推进。本轮没有产品实施授权，工程验证未运行不算通过。执行拆分另发现一处现行框选Ctrl键的文码差异，已在 [T-GEST-01](execution/04-Gestures-and-Selection-Tools.md)列为实施前追溯／消歧门；不因“沿用现有”就认定源码可覆盖SRS，确需改变可见键义时再问。R21若可独立复现修复，可放进A1，不必等新手势整轮。

### B：工作区状态专题（R06/07）

这是单独一次中优先级、多阶段开发，建议至少三个独立验收切片：

1. B1：按04已确认表A/B落实白名单／所有权，并实测冻结presentation字节／条目及恢复资源预算；同Track共用profile、跨Track隔离，关闭Tab留轻量状态，删除owner释放自身状态。主Piano／事件Snap独立；Lane局部轴／顺序按Segment；存活owner可留有预算的dormant Onion来源ID；共享主ToolMode和独立事件绘线形态已按Q2确认，不拆两套主ToolMode。
2. B2：新 presentation schema、普通 Save / Save Copy 冻结、Mute/Solo 恢复、旧格式 / 损坏隔离。
3. B3：全部 Workspace 的 Tabs / 顺序 / 子页 / 定位恢复、懒加载、长会话订阅与内存回归。

R28 必须先固定目标 Tab 身份。R30 尚未做时预留 profile 扩展点即可，不因此阻塞 B；未来新面板再进入白名单。

### C：多实例与跨项目剪贴板专题（R01）

独立于 B，不机械要求 B 全做完，但 B/C 的同文件保存决策应共用：

1. C1：按已定同ProgramRoot文件型方案，落实对象矩阵／TPQN／源退出及跨重启生命周期；按已确认的D-MI06.a/b单槽／升级策略，冻结协议、安全预算、共享存储／清理／文件写保护。不同ProgramRoot配置及音乐clipboard均隔离。
2. C2：多实例启动隔离；同 TPQN 的基础 Note/Event 跨项目、跨实例分块传输与原子粘贴。
3. C3：已批准覆盖全部现有可复制对象，落实复杂依赖闭包、共享Root/Usage、新Root默认Auto；基础对象／适用Pure MIDI支持跨TPQN换算，复杂逻辑依赖仍限同TPQN。首个同TPQN基础切片不代表最终范围已完成。
4. C4：真实双 / 多进程、源退出、传输中断、损坏输入、并发保存、缓存回收和百万级取消 / 性能门。

C1 未通过不得仅删除 Mutex 发布“多实例支持”。

### D：低优先级与待准备事项

- R04：统一打开入口可提前独立做；多实例发布时再接入选窗策略。
- R03：发布前原生窗口专项，可可靠回退／不做；Win10只能安排用户可提供的VM，不能写实机通过。R08：局部图标已由D-UI01.a/04指定，其余等用户准备。R09：已获补键授权，实施前冻结冲突检查后的键表，不改旧键位、不扩可配置系统。
- R10/11：Q1／Q2行为／默认／衔接已定，连续跟随纳入Arrangement／Conductor，但琴键染色仍仅两类Segment及All Tracks；先做各自冷／热缓存性能对比。不能用小样本替代大型MIDI，也不以像素读取代替Gate正确性。
- R30：Tracks 面板复用专项，可在状态专题前或后，避免复制 Arrangement 业务代码。
- R02：正式发布且稳定后再做，已定轻动效、总关闭、亚克力默认关、就绪动效默认开；无缓存等待区静态棋盘占位，已有粗内容保留且粗→细无动画；非近期任务。

## 6. 每轮交付和共同验收门

每轮输出：R-ID 改动清单、决策落实表、源码 / SRS / ADR 追踪、自动测试证据、失败项和限制、用户可勾选人工清单。不得以“整体通过”替代未跑的边界测试。

自动验证最低维度：

- 视图：MIDI Segment / Logical Segment / SubVoice；关联 Velocity / 参数 / MIDI Event / Tempo；不能只修一个宿主。
- 内容：空、单点、10 对象、稀疏 / 密集、同 Tick 重复、跨页、40 万选择、百万级对象、大型样本；大小规模命令语义一致。
- 编辑：创建、普通 / Ctrl 复制拖动、左右 resize、Properties、批处理、列表入口、Undo/Redo、精确碰撞、取消、旧 revision、Owner 删除。
- 渲染：冷 / 热页、边界外内容、缩放切换、选择层对齐、非空首帧、后台取消；保留已验收的局部失效 / 两级 Arrangement 概览策略。
- 性能：相同机器 / 样本 / 设置分别测冷暖 p50/p95/p99 交互耗时、UI 最长停顿、首内容时间、分配量、GC heap、Working Set、resident/spill/队列上限和关闭释放。不得只报单次平均值。
- 大型样本优先沿用 `9KX2 18 Million Notes.mid` 的 MIDI Out #23 密集区和 40 万 / 百万选择；运行该实验时须先确认样本路径存在。MIDI→Logical→SubVoice 路径不能省略。正式运行以进程树内存接近 9 GiB 为警戒并终止有失控趋势的探针，避免系统分页假死。
- 持久化 / 声音相关变更：Full/Incremental 等价、正式序列与 source trace、Format 1/2/3 重开、Save/Save Copy、失败原子性、试听停止所有权；UI 名称、颜色、Tab 重排不改 canonical/导出。
- UI 人工只做自动测试无法覆盖的布局、视觉、手感和声音确认；每轮给稳定编号小清单，不要求用户反复全软件遍历。DPI 100/125/150/200%、短窗、长名称、禁用状态作为统一视觉矩阵。

性能验收基线应在每个专题实施前重测，预算和可接受退化明确记录；本轮不承诺未经测量的百分比收益或耗时。不得为 R31 百分比、R28 Tab 数或 R11 染色增加全工程 UI 同步扫描。

## 7. 文档收尾状态

- 已逐条归档 32 项原始要求，保留低 / 中 / 高 / 最高及等待条件。
- 已对关键共享 Surface、命令、Catalog、预览、启动 / 剪贴板 / presentation 路径做只读源码调查。
- [04](04-Decisions-and-Preparation.md)的95项Q1和9项Q2均有回答，逐项保留原问题／推荐／分歧／用户原答，更新当前归纳与状态；D-UI03为既有规则、ENV-WIN10为已回答资料，不计入104项。
- 01～03同步实际决定，[05汇总](05-Decision-Summary-and-Q2-2026-09-14.md)已就地更新；04继续作为唯一回答入口。9项Q2按用户明确回答归并，未发现必须Q3，未擅自把历史推荐覆盖新答。
- 用户原草稿 `misc/user/midora-plan-pre-v1.0.txt` 已在此前按用户要求归档跟踪，本次不修改其内容。
- 本轮先完成归档提交／推送`7227090`，再做本目录Q2决策归并；后续改动留工作区。校验编号、原答、链接及一致性，不改产品、SRS、版本或用户草稿，不运行发布。
- 后续用户要求的主题执行拆分已写入`execution/`；本次新增规划未提交／推送，原Q1/Q2问题和回答未改，未开始产品实施。工程验证与人工验收均为计划，不宣称已完成。
