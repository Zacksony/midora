# 完整决策与文档问答清单

首版日期：2026-09-11。最近归并：2026-09-14。问卷修订：**Q1 的 95 项与 Q2 的 9 项均已回答并归并** 。

状态：**104 项已按原答收敛为确认／被后续修订取代／不适用；本轮未发现必须追加的 Q3，产品实施未开始** 。

本文件是本批需求的唯一决策问答入口。覆盖前述完整聊天清单及所有已知待讨论问题，**不包含 R08 图标选择** 。原始 32 项要求、优先级与交付建议见 [00：总表](00-Overview-and-Delivery-Plan.md)；源码审计、风险和验证细节分别见 [01](01-Interaction-and-Display.md)、[02](02-Instrument-Changes-and-Lanes.md)、[03](03-Multi-Instance-and-Workspace-State.md)。

2026-09-14 已先将 Q1 原答归档为 `3619dee`；本轮又按用户要求，先将 Q1 归并文档及 Q2 全部原答提交、推送为 `7227090`，再进行查漏与决策同步。当前新增整理留工作区，不再次自动提交／推送；不实施产品、不改 SRS／ADR／版本、不发布。当前 SRS 仍为正式基线，已确认的新要求需在相应实施阶段按变更流程落实。

**阅读优先级：** Q1／Q2 的“具体问题／推荐方案／分歧”均保留作原问卷历史，不因归并重写；当前决定看“用户回答＋补充问答＋确认状态”，便览见 [05：决策汇总](05-Decision-Summary-and-Q2-2026-09-14.md)。各轮“同意”只覆盖该轮该项推荐；Q2 的明确修订覆盖冲突的 Q1 旧答，不靠批量同意批准未来新问题。

## 0. 如何在本文问答

### 0.1 记录规则

1. 保留已有父编号，用 `.a`、`.b` 拆分；每项先写明确的“具体问题”及必要场景，说明需要用户决定什么，再分别列推荐、分歧／代价、用户回答、补充问答和确认状态。不能只写标题和方案、让用户自行推测问题；已有编号不回收、不改义。
2. 用户可直接填写各项“用户回答”，或在下方统一答复区写“除……外，Q1 全部采用推荐”。也可以按父编号批准本次修订下的全部子项；特定子项答复优先于父项／统一答复。
3. 未填、空白、暂缓、只是提出疑问都不等于批准。推荐、已确认、已同步规格、已实施、已验收是不同状态，不得合并。
4. 后续提问、回答和结论默认在文档内继续；聊天只提示文档入口、需回答的编号及重要变动。用户在聊天中明确答复时，也应原意记录到相应项，保留答复来源／日期。
5. 不改写用户原回答。补充解释、实现者结论和追问写在“补充问答”；明确有歧义或跨项冲突时先标为“讨论中”，不得替用户决定。
6. “全部推荐”只覆盖明确对应的问卷修订，不自动批准之后新增／改变的推荐。后续新增实质决策使用新编号并写变更记录；已答条目修改推荐时保留前后依据并重新确认受影响部分。
7. 技术测试、页长／缓存预算等由实现者调查确定，不伪装成产品选择，也不把没有运行的测试写成通过。发现真实新约束后允许补充提问，不承诺本问卷能预知所有技术结果。
8. 图标准备继续由 R08 独立维护，不放进本次问卷，也不包含在“Q1 全部推荐”授权中。

### 0.2 用户统一答复区

**用户回答：** 只要我答了“同意”的，都等同于“同意《推荐方案》”。

**补充问答：** 2026-09-14 已核对 Q1 95 项及 Q2 9 项，均有回答。“同意”解释为同意所答修订的该项推荐；同一回答内的明确修订优先。Q1 的 D-LANE01.a 讨论已由 Q2 D-LANE01.g 收敛，D-IN01.d 前提仍已撤回；Q2 D-UI01.d／D-VIS02.f 按明确修订归并，并非原推荐照单批准。未来 Q3 不受本轮批准自动覆盖。

**确认状态：** 已确认统一解释规则；原答保留。

### 0.3 快速导航

- [鼠标交互、浮动工具及基础显示](#2-鼠标交互浮动工具及基础显示)
- [乐器变化点与试听](#3-乐器变化点模型导入编辑与试听)
- [Lane Tabs、友好数值与阶梯线](#4-lane-tabs友好数值与阶梯线)
- [连续跟随与琴键染色](#5-连续跟随与播放琴键染色)
- [Tracks 侧栏与文件打开](#6-segment-tracks-侧栏与文件打开)
- [工作区恢复与同轨道共享](#7-完整工作区恢复及同轨道共享设置)
- [多实例与跨项目剪贴板](#8-多实例与跨项目剪贴板)
- [快捷键、原生窗口与动画](#9-快捷键windows-窗口效果与后续动画)
- [无需用户选择的验证门](#10-必须安排的技术验证不是待用户拍板的实现细节)
- [环境资料与后续新增问题](#11-环境资料与后续新增问题)
- [Q2：9 项原问题、原答与确认结论](#13-q2-补充问题2026-09-14)
- [Q1／Q2 当前决定及技术答复](05-Decision-Summary-and-Q2-2026-09-14.md)

## 1. 不需要重新确认的要求与原推荐更正

### 1.1 已明确方向

- R01～R32 的原优先级、全部目标及等待条件不丢失；R02 动画在正式发布稳定后，R10／R11 先测性能。
- R18 取消右双击、右拖框选、右单击不再人为等待；Q2 已改为保留共享 Draw，数值事件区增加 Free／Line／Horizontal 绘线形态工具组，非 Draw 时禁用。
- R06 视图及 Track Mute／Solo 随显式保存，不进入 Undo／不标音乐 Modified；具体白名单与组状态见 D-STATE。
- R27 使用名称与数值选择，默认试听 Key 60／Velocity 100；保留 Pure MIDI 原始事件，Logical Segment 不增加 Instrument Changes。
- R28 按 target 独立 Tab，Vel. 第一、Inst. 适用时第二且常驻；普通 Tab 可隐藏、重排，右侧下箭头为可搜索的全部目标目录，显示精确计数与显隐。计数摘要后台／增量维护，不在打开目录时全量扫描事件。
- R14 SubVoice 全创建路径自动扩模板；R23 Create 短文案／Browse 加宽；R24 成功后激活新 Lane 并恢复编辑焦点；R25 三类 List 默认 400px；R17 禁止下拉内容边缘悬停自动滚动；R26 去无用编号但保留必要 target 身份。
- 时间尺跳标从全局起点锚定，不随 viewport 改变相位；原 Follow 默认关闭。
- Note 精确同 start tick＋key 后来者删除；事件精确同 tick＋正式 target 后来者覆盖；未被相关编辑命中的导入重复保留。
- 正确性、确定性、稳定 ID、半开范围、有界分页、原子提交、取消及 Undo／Redo 不属于可省略项。UI 名称、分组显示、Tab 顺序不得另建音乐语义。

### D-UI03 — 沿用既有共同 delta 饱和（不计入待答 95 项）

SRS §20.1.7 已规定批量事件移动采用整组共同受限 delta：任何点先触边，整个形状停止继续越界。R13 未要求改变形状，按此修复 Direct MIDI 与其他两类的一致性即可，不是 A1 阻塞项。若用户主动要求各点分别 Clamp、允许形状压平，需明确新增需求决定。preview、delta 文本和提交必须一致，先在标量值上变换再编码 MIDI 字节。

### 1.2 Q1 提问时的推荐更正（历史，不覆盖后续用户回答）

下列记录说明 Q1 相对初稿的变化，不代表当前全部采用。特别是导入 PC 投影已被 D-IN01.c 明确否决；当前结果以各项原答及 05 汇总为准。

- D-UI02：从内容区 Ctrl+左单击改为**顶部时间尺** Ctrl+左单击，避免抢占既有多选／复制拖动。
- D-IN02.a：仅改成员值且结构仍完整时保留包装；不再“一改 raw 就解组”，按完整事务最终态判断。
- D-IN01.c／d：导入 PC 可在新 Tab 投影、编辑，但不自动认领／改写原有 Bank 关系；不能把“导入不自动包装”误写成“导入 PC 不显示”。
- D-IN05.d：试听参数记忆的新推荐为程序级，取代初稿的仅会话内候选。
- D-LANE01.f／D-STATE03.a：保留钢琴卷帘与事件独立 Snap；Lane 纵轴／排列候选归属 Segment，不是同 Track 实时联动。
- D-MI03／04：分别列首切片与最终范围；不能将同 TPQN 基础对象首切片冒充完整跨项目能力。
- 下文将原“以后再决定”的已知产品分歧全部展开；剩余技术事实只能由后续实验确认。

## 2. 鼠标交互、浮动工具及基础显示

关联 R05、R13～R26、R31～R32。这里不重新询问用户已明确的取消右双击、右拖框选等目标，只冻结其配套规则。

### D-UI01.a — 绘制形态与命中优先级

**具体问题：** 右键拖动改为框选后，原来的自由画线、直线和水平线应通过什么入口操作？是否在 Draw 中提供三种绘制形态，并保留“命中已有点先移动、Alt+左拖强制绘制”的区分，而不再占用右拖？

**推荐方案：** Draw 增加 Free / Line / Horizontal 三种绘制形态，左拖执行。普通左键命中已有点仍用于移动；Alt+左拖可在密集点上强制绘制。原有 Shift 锁 Tick／单点创建语义保留。应用于适用的事件、参数、Velocity、Tempo 视图。

**分歧／代价：** 也可以继续用修饰键区分直线、水平线，但组合更难记，也更容易冲突。

**用户回答：**

同意推荐方案。

不过，准确来说，是把原先的Draw工具删掉，然后再增加Free / Line / Horizontal Pen三种工具。

Free按钮用原先Draw工具的图标；

Line按钮用LineFilled；

Horizontal按钮用SubtractFilled。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：本项 Q1 原答中“删除 Draw、三个独立 Pen”已由 Q2 D-UI01.d 明确撤回。最终保留共享主 ToolMode 和 Draw；只在适用的数值事件区增加 Free／Line／Horizontal 绘线形态组，非 Draw 时禁用。保留原命中移动、Alt 强制绘制和 Shift 语义；三个已指定图标继续用于形态按钮。

**确认状态：** 已由 Q2 D-UI01.d 修订并确认；不能按本项历史原答删除 Draw。

### D-UI01.b — 右拖框选的覆盖面

**具体问题：** 右键拖动框选是否统一覆盖 Arrangement、三种钢琴卷帘、事件／参数、Velocity 和 Tempo 的可编辑内容区？其中 Velocity 框选对应音符；轨道头、标尺、琴键及只读 All Tracks 是否明确排除？

**推荐方案：** 右拖固定框选，覆盖 Arrangement 内容区、三种钢琴卷帘、数值事件视图、Velocity 和 Tempo；Velocity 框选对应音符。保留现有追加／减选等修饰键规则。轨道头、大括号、标尺、琴键、只读 All Tracks 不纳入此规则。

**分歧／代价：** 不建议为某种可编辑点视图保留右拖绘线例外。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：右拖固定框选，覆盖 Arrangement 内容区、三种钢琴卷帘、数值事件视图、Velocity 和 Tempo；Velocity 框选对应音符。保留现有追加／减选等修饰键规则。轨道头、大括号、标尺、琴键、只读 All Tracks 不纳入此规则。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-UI01.c — 冷数据命中时的右键菜单

**具体问题：** 如果右单击时鼠标下的数据尚未从磁盘读取完，菜单应立即出现并显示暂不可操作的“定位中…”，还是等命中查询完成后再出现完整菜单？这里讨论的是实际数据等待，不是已决定取消的人为双击等待。

**推荐方案：** 右单击取消人为双击等待。极端情况下冷数据命中查询未完成，立即显示禁用的“定位中…”菜单框架，就绪后填充冻结目标的菜单。

**分歧／代价：** 或等后台命中完成后才显示完整菜单。前者反馈更及时，但偶尔会看到加载状态；不能在 UI 线程同步读冷页，也不能用旧选择误开菜单。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：右单击取消人为双击等待。极端情况下冷数据命中查询未完成，立即显示禁用的“定位中…”菜单框架，就绪后填充冻结目标的菜单。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-UI02 — 保留选择而移动编辑指针

**具体问题：** 已经选中一批对象、但只想把蓝色编辑指针移到别处而不丢失选区时，应使用什么手势？是否采用 Ctrl+左单击顶部时间尺，并保留内容区 Ctrl 操作原有的多选／复制拖动用途？

**推荐方案：** Ctrl+左单击顶部时间尺只移动蓝色编辑指针，保留选择。普通时间尺单击仍移动播放指针；内容区 Ctrl 多选、复制拖动不变。

**分歧／代价：** 这取代初稿中的内容区 Ctrl+左单击方案。另一种是改变内容区普通单击的清选习惯，不推荐。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：Ctrl+左单击顶部时间尺只移动蓝色编辑指针，保留选择。普通时间尺单击仍移动播放指针；内容区 Ctrl 多选、复制拖动不变。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-UI04 — 浮动工具内容及跨模式显示

**具体问题：** 选择浮动工具是否保留现有移动、左右边界调整及 Follow／锁定等控件，只新增常驻的 Copy、Cut、Delete，将其他批量操作留在菜单中？有效选区存在时，它是否应跨 Draw／Select 等模式保持显示；还有哪些操作需要常驻？

**推荐方案：** 保留移动、左右边界调整、Follow／锁定和拖动区域；新增常驻 Copy／Cut／Delete，其余批量操作放入工具菜单。右拖产生有效选择后，所有编辑模式都可显示该工具。

**分歧／代价：** 若希望其他操作也常驻，请指定最常用的几个；全部铺开会明显增加遮挡。沿用正式同质／兼容选择和预览能力，不以新增工具绕过类型边界。

**用户回答：**

同意。新增的工具：Copy／Cut／Delete，左右翻转、上下翻转、Scale、移调、Batch Edit、类人化、Split、Join、量化、Properties。

每个工具的图标（这也决定了一部分菜单项的图标）：

Copy：CopyRegular

Cut：CutRegular

Delete：DeleteRegular

左右翻转：FlipHorizontalRegular

上下翻转：FlipVerticalRegular

Scale：ScaleFitRegular

移调：ArrowMaximizeVerticalRegular

Batch Edit：DocumentSettingsRegular

类人化：AnimalPawPrintRegular

Split：SplitVerticalRegular

Join：SquareDovetailJointRegular

量化：TableMoveLeftRegular

Properties：SettingsCogMultipleRegular

**补充问答：** 2026-09-14 归纳（非改写用户原答）：保留现有 Move、左右 Resize、Follow／锁定与 grip；新增常驻 Copy、Cut、Delete、左右／上下翻转、Scale、移调、Batch Edit、Humanize、Split、Join、Quantize、Properties。有效选择跨模式显示，仍按对象兼容性启用；具体图标见本项用户原答及05 §2.9。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-UI05 — 模板长度手柄的下界

**具体问题：** 向左拖短事件乐器的模板长度时，如果会切入已有内容或违反 Loop／Pre-Roll 的合法下界，手柄应直接停在最短合法位置，还是允许预览越界、松开后拒绝修改？

**推荐方案：** SubVoice 模板长度手柄达到合法最小长度时停止缩小，显示实际生效的 delta，不报错。最小长度受内容、Loop、Pre-Roll 等正式约束影响。

**分歧／代价：** 或拖到非法位置后拒绝提交。不能借此把模板内容静默藏到范围外。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：SubVoice 模板长度手柄达到合法最小长度时停止缩小，显示实际生效的 delta，不报错。最小长度受内容、Loop、Pre-Roll 等正式约束影响。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-UI06 — 编译百分比

**具体问题：** 编译的某些阶段无法在不额外扫描全部数据的情况下得到准确总量，此时应只显示阶段名／已处理量，还是显示估算百分比？是否接受仅在确有可靠总量的阶段显示确定百分比？

**推荐方案：** 存在可靠总量时显示百分比；无法可靠计量的阶段显示阶段名称。不得为了得到总量额外预扫描整个大型项目。

**分歧／代价：** 若要求始终显示百分比，需要接受“估算进度”，不等于实际剩余耗时。

**用户回答：** 同意。如果测试了性能后发现编译速度仍然倒退过多到无法接受，那么这个需求可以不实现。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：有可靠 total 才显示百分比，否则显示阶段；不为计量额外全量预扫描。用户允许在实测有不可接受的编译时间倒退时放弃此需求。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-UI07 — All Tracks 首次适配与小窗口

**具体问题：** All Tracks 首次适配高度时，如果窗口放不下 128 个各高 4px 的琴键，应保留每键至少 4px 并允许垂直滚动，还是继续缩至 3px 以显示更多音高？

**推荐方案：** 首次适配时优先保证 N ≥ 4 pixels/key。窗口内容高度不足以放下 128 个键时，允许垂直滚动。按既有设备像素规则扣除标尺等非内容区域后计算。

**分歧／代价：** 或小窗口退至 3 pixels/key，优先显示更多键。

**用户回答：** 修正：需求应改为 >= 3 pixels/key，不是 4。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：改为 ≥3 device pixels/key；首次有效布局取最接近完整显示 128 键的整数高度，小窗口保留 3px 下限并允许滚动，不再要求 ≥4。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-UI08.a — 播放中 SoundFont 快捷入口

**具体问题：** 正常项目播放期间，底部 SoundFont 快捷入口应禁用，还是允许打开仅供查看、不能提交修改的音色库设置？这两种做法都不应因为点击该入口而自动停止正常播放。

**推荐方案：** 底部 SoundFont 入口直达设置的 SoundFonts Tab；正常播放期间继续遵守现有设置禁用规则，不自动停止项目播放。

**分歧／代价：** 若希望播放时也能点开，可以新增只读浏览状态，工作量略增。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：底部 SoundFont 入口直达设置的 SoundFonts Tab；正常播放期间继续遵守现有设置禁用规则，不自动停止项目播放。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-UI08.b — 按项滚动的步长

**具体问题：** 对于要求按列表项滚动的 Bank／Program／扫描结果等音色列表，每次鼠标滚轮一个刻度应移动 1 项、3 项，还是允许配置？内部列表滚动仍不得带动外层滚动。

**推荐方案：** Catalog／Bank／Program／Scan Presets 等按项滚动的列表，每个标准滚轮刻度滚动 1 项；高精度滚轮累计不足一项的余量，列表内滚动不带动外层。

**分歧／代价：** 可改为 3 项或指定其他数量。这里不是用像素步长代替逻辑项滚动。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：Catalog／Bank／Program／Scan Presets 等按项滚动的列表，每个标准滚轮刻度滚动 1 项；高精度滚轮累计不足一项的余量，列表内滚动不带动外层。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-UI09 — 浮动拖动出界后的持续滚屏

**具体问题：** 拖动浮动工具中的移动／长度手柄到视图边缘外，并将鼠标停住时，是否应持续自动滚屏并更新拖动 delta，直到回到视图内或松开，而不是只在鼠标继续移动时滚动？

**推荐方案：** 浮动工具拖动到视图边缘外时，即使鼠标停住，也持续自动滚屏并更新操作 delta，直到松开。

**分歧／代价：** 另一种是仅在鼠标继续移动时更新；拖出很远时不够方便。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：浮动工具拖动到视图边缘外时，即使鼠标停住，也持续自动滚屏并更新操作 delta，直到松开。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

## 3. 乐器变化点：模型、导入、编辑与试听

关联 R27（P0）、R28，并影响 R06／R01 的持久化及剪贴板。当前用户决定见各项补充问答；本轮尚未改写正式 source/schema。

### D-IN01.a — 持久编辑分组与音乐事实

**具体问题：** 一个“乐器变化点”需要将 Bank MSB、Bank LSB 和 Program 作为整体编辑并在重开后保持关联。应保留底层原始事件、另存它们的稳定编辑分组，还是新增一种由编译器解释的正式音乐事件，或仅保存可丢弃的视图分组？

**推荐方案：** 原始 MIDI／SubVoice 事件仍是唯一音乐事实；乐器变化点是关联稳定成员 ID 的持久编辑分组。不重复储存第二份可独立修改的 Bank／Program 数值，不新增由编译器解释的音乐事件类型。分组关系随正式编辑进入 Undo／Redo，并保存。

**分歧／代价：** 仅存 presentation 更轻，但旧版保存或 presentation 丢失后包装可能消失；新增正式 InstrumentChange 音乐对象则改动范围最大。分组自身也需要稳定身份，不能用数组下标或 Tick 代替。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：原始 MIDI／SubVoice 事件仍是唯一音乐事实；乐器变化点是关联稳定成员 ID 的持久编辑分组。不重复储存第二份可独立修改的 Bank／Program 数值，不新增由编译器解释的音乐事件类型。分组关系随正式编辑进入 Undo／Redo，并保存。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN01.b — 格式与兼容性代价

**具体问题：** 如果要让乐器变化点的成员关联可靠保存，而现有项目格式无法完整表达，是否接受必要的格式扩展及旧项目迁移，继续遵循确认并备份后覆盖保存的规则？还是宁可限制为视图分组，以减少音乐源数据格式变化？

**推荐方案：** 必要时升级编辑组织数据／项目格式，新版本继续读取旧项目；迁移保存沿用“确认＋自动建立旧版副本”的流程。不承诺旧软件能读取新增的正式源格式。

**分歧／代价：** 若要求尽量不触及源格式，应选择仅 presentation 包装，并接受其可丢失性。扩充独立 presentation 不机械要求升级外层 Project Format，不在问答阶段提前确定版本号。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：必要时升级编辑组织数据／项目格式，新版本继续读取旧项目；迁移保存沿用“确认＋自动建立旧版副本”的流程。不承诺旧软件能读取新增的正式源格式。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN01.c — 导入 PC 在新 Tab 的可见性

**具体问题：** 导入 MIDI 后，原有 Program Change 及其对应 Bank 尚未被用户创建为整体包装时，新 Instrument Changes Tab 是否也应显示它们？还是只显示用户在新界面显式创建的乐器变化点？

**推荐方案：** 导入 MIDI 后，Instrument Changes Tab 也显示已有 Program Change。每条 PC 形成一个只读关联意义上的显示投影，按正式顺序查询该 PC 使用的 Bank；不改写原事件，也不把前面的 Bank 消息强行认领为其独占成员。

**分歧／代价：** 仅显示通过新入口创建的完整包装最简单，但导入 MIDI 的新 Tab 可能空白。只读的是关联投影而不是禁止编辑 PC，具体编辑见 D-IN01.d。

**用户回答：**

导入 MIDI 后，Instrument Changes Tab 永远为空。不需要做额外转换和读取，更不要额外建一个只读语义。

导入 MIDI 后的 PC/Bank 只能在对应 Lane 中单独编辑。

这个 Tab 的对象只能在 Midora 项目中显式添加，先不打算从 MIDI 文件转换。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：导入 MIDI 不生成、发现或投影 Instrument Changes 包装，Inst. 初始为空。导入 Bank／PC 只在原始 Lane 编辑；之后仍允许用户显式添加包装。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN01.d — 编辑与删除导入 PC 投影

**具体问题：** 导入 MIDI 中，一个早先的 Bank 事件可能影响多个后续 Program Change。用户在新 Tab 修改其中一个音色时，是否应只在该 Program Change 处形成局部完整组合、保留早先 Bank；删除时只删除该 Program Change？还是将这类导入投影设为只读，必须回原始事件入口编辑？

**推荐方案：** 导入 PC 投影也能打开选择器编辑。选择完整音色后，在该 PC 所在位置形成完整的 Bank→PC 组合；不修改此前可能被其他 PC 共用的 Bank。删除未包装投影时，只删除对应 PC。界面区分“完整乐器变化”与“独立 Program Change”。

**分歧／代价：** 或让导入投影只读，必须转到原始 Lane 编辑；更简单但不够统一。

**用户回答：** 不适用。新 Tab 里的对象完全不感知导入 MIDI 后的事件。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：不适用：D-IN01.c 已取消导入 PC 投影；不再实施其投影编辑／删除分支。

**确认状态：** 不适用：导入 PC 投影前提已由 D-IN01.c 取消。

### D-IN02.a — raw 编辑后何时解除包装

**具体问题：** 一个已包装的乐器变化点被原始事件编辑入口修改后，什么时候应解除其整体关联？是任何成员改动都解除，还是只在部分移动／删除等操作破坏组合结构时解除，而单纯改成员数值仍保留？

**推荐方案：** raw 成员仅改数值，且仍满足同 owner、同 Tick、正确 target 关系时，保留包装并更新标签。部分移动、删除、改 target 等导致结构不完整时解除包装，剩余 raw 保留。以整个事务最终状态判断，整组移动不能中途误解组。

**分歧／代价：** 取代初稿的“单独改值也解除包装”。不建议自动补回被删成员，也不建议改一个成员就连带修改其他成员。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：raw 成员仅改数值，且仍满足同 owner、同 Tick、正确 target 关系时，保留包装并更新标签。部分移动、删除、改 target 等导致结构不完整时解除包装，剩余 raw 保留。以整个事务最终状态判断，整组移动不能中途误解组。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN02.b — 新建与导入编辑的同 Tick 插入位置

**具体问题：** 新建乐器变化点与 NoteOn 落在同一 Tick 时，是否应排在该轨道的新 NoteOn 前，使新音符使用新音色？对于修改导入的既有 Program Change，是否仍保留它原本的同 Tick 次序，而不一律提前？

**推荐方案：** 新建完整乐器变化默认使本轨该 Tick 新触发的音符采用新音色，Bank→PC 放在相应 NoteOn 前。编辑导入的既有 PC 则保留原来的顺序位置，不擅自提前。

**分歧／代价：** 不能保证影响同 Root 中、正式同 Tick 排序更靠前的其他轨道音符；那由跨轨道顺序决定。实现前必须将此规则与正式 order 的维护一起落实，不能由 UI 私自重排 canonical。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：显式新建包装的 Bank→PC 在本轨同 Tick NoteOn 前，不追溯影响正式顺序更早的其他轨道。导入 raw PC 保留原顺序；已取消的导入包装编辑分支不实施。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN02.c — 包装的批量操作覆盖面

**具体问题：** 整体乐器变化点是否支持复制、删除、水平移动／翻转、Scale、量化和多选属性，但不套用针对单个数值的普通 Batch Edit、垂直翻转或移调？是否还需要专门针对 MSB／LSB／Program 三字段的表达式编辑能力？

**推荐方案：** 完整包装支持复制、剪切、粘贴、删除、水平移动／复制拖动、水平翻转、Scale、量化、多选 Properties。没有纵向移动、移调、音符 Split／Join。

**分歧／代价：** 普通事件 Batch Edit 的单一 Point Value 无法表示三元组，因此不直接套用；若需要表达式批改 MSB／LSB／Program，应另设明确的三字段能力。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：完整包装支持复制、剪切、粘贴、删除、水平移动／复制拖动、水平翻转、Scale、量化、多选 Properties。没有纵向移动、移调、音符 Split／Join。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN03.a — SubVoice 独立 Bank／PC 的维护入口

**具体问题：** SubVoice 的普通创建入口改用完整 Bank／Program 选择器后，已有的独立 Bank、独立 Program、部分组合及 Mapping 应在哪里维护？是否保留明确的高级原始事件入口，而不是彻底取消独立字段编辑？

**推荐方案：** SubVoice 普通新增入口以完整乐器变化为主；保留高级 raw 入口，维护旧的独立 Bank／PC、部分字段和精确 Mapping 目标。

**分歧／代价：** 彻底禁止独立 Bank／PC，需要完整替代入口，否则会让已有合法数据不可编辑。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：SubVoice 普通新增入口以完整乐器变化为主；保留高级 raw 入口，维护旧的独立 Bank／PC、部分字段和精确 Mapping 目标。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN03.b — Initial State 的部分继承

**具体问题：** Initial State 将 Bank／Program 合成一行后，是否仍允许 MSB、LSB、Program 各自继承或覆盖，只在选择完整音色时一起赋值？还是要求三个字段只能整体继承或整体覆盖？

**推荐方案：** Initial State 合并成一行选择器，但高级区域保留每个字段的“继承／覆盖”。选择完整音色时才覆盖三字段；仅打开、浏览、取消不得补 0。

**分歧／代价：** 改成只能完整覆盖会丢失现有部分继承能力，不推荐。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：Initial State 合并成一行选择器，但高级区域保留每个字段的“继承／覆盖”。选择完整音色时才覆盖三字段；仅打开、浏览、取消不得补 0。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN03.c — 统一选择器的 Initial State 范围

**具体问题：** 统一音色选择器用于 Initial State 时，本批是否只覆盖 Event Instrument 全局和 SubVoice 两级，还是也要覆盖 Project Global Initial State？

**推荐方案：** 这次统一 Event Instrument 全局及 SubVoice Initial State；不顺带改 Project Global Initial State。

**分歧／代价：** 若希望三处全部统一，可在此批准扩大范围；仍需保留对应继承语义。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：这次统一 Event Instrument 全局及 SubVoice Initial State；不顺带改 Project Global Initial State。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN04 — Program 显示编号

**具体问题：** Program 在所有选择器、属性和提示中应统一显示 MIDI 原始的 0～127，还是面向用户的 1～128？例如底层 Program 0，应显示为 0 还是 1？

**推荐方案：** 所有相关数值入口统一显示 0～127，明确标注 Program (0–127)。同时更正旧规格中曾出现的 UI 1～128 口径。

**分歧／代价：** 也可以统一为 1～128，但必须所有入口一起转换，不能混用；底层仍为 0～127。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：所有相关数值入口统一显示 0～127，明确标注 Program (0–127)。同时更正旧规格中曾出现的 UI 1～128 口径。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN05.a — 自动试听时长与快速换选

**具体问题：** 在音色列表换选时，自动试听应播放多长时间，是否采用可关闭的 300ms 短音、快速换选只试听最新项？手动按住试听琴键时，是否改为持续发声直到松开？

**推荐方案：** 自动试听默认短音 300ms，提供开关；快速换选只保留最新请求。手按琴键按住持续、松开结束 Gate。

**分歧／代价：** 时长可以改；不建议默认无限持续试听。该 300ms 与要取消的右双击等待无关。

**用户回答：** 同意，不过默认短音要 500ms。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：自动试听默认短音 500ms，可关闭；快速换选只保留最新请求。手按琴键持续到松开 Gate。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN05.b — 试听上下文

**具体问题：** 音色选择器的试听应使用干净的初始通道状态，方便听清音色本身，还是还原当前项目／Root 的 Expression、Pitch Bend、Mapping 等上下文，以试听当前位置的实际效果？

**推荐方案：** 使用干净控制器状态的独立音色试听，不套用当前时间线的 Expression、Pitch Bend、Mapping 等状态。明确这不是当前编曲听感的完整重现。

**分歧／代价：** 按当前 Root／时间线完整上下文试听更贴近实际，但实现、状态查询和验证明显更重。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：使用干净控制器状态的独立音色试听，不套用当前时间线的 Expression、Pitch Bend、Mapping 等状态。明确这不是当前编曲听感的完整重现。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN05.c — 试听 Channel Mode

**具体问题：** 试听时是否允许临时切换 Melodic／Percussion，且只影响试听、不修改项目的 Channel Mode？还是要求试听始终跟随目标的初始通道模式？

**推荐方案：** 试听提供 Melodic／Percussion 选择，初值取已知目标初始模式；此选择只影响试听，不修改 Root 或写入项目 SysEx。

**分歧／代价：** 只跟随初始模式更简单，但不便浏览另一类音色。Root 初始 Mode 不能冒充中途 SysEx 之后的当前活动 Mode。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：试听提供 Melodic／Percussion 选择，初值取已知目标初始模式；此选择只影响试听，不修改 Root 或写入项目 SysEx。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN05.d — 试听参数记忆范围

**具体问题：** 试听的 Key、Velocity、时长和自动试听开关，应记为程序级偏好，在下次弹窗乃至重启后沿用，还是仅在当前会话内记住？

**推荐方案：** 自动试听开关、Key、Velocity、时长保存为程序偏好，不进入项目。首次仍为 Key 60／Velocity 100。

**分歧／代价：** 原文档建议仅会话记忆；程序级记忆更方便，但需要新增设置字段。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：自动试听开关、Key、Velocity、时长保存为程序偏好，不进入项目。首次仍为 Key 60／Velocity 100。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-IN05.e — 音色列表的名称来源

**具体问题：** 音色选择器应只展示当前有效 Catalog 的名称，还是也允许浏览已禁用音色库的目录并标明不可用于当前试听？无论哪种，是否保留不依赖目录名称的合法 Bank／Program 数值输入？

**推荐方案：** Bank／Program 列表优先展示现有 Catalog resolver 解析出的有效音色表；名称缺失仍可输入任意合法数值，不限制只能选“已知音色”。

**分歧／代价：** 若希望同时浏览未启用音色库的 Catalog，应额外区分“可浏览”与“当前可试听”。沿用现有 resolver 优先级，不把 Catalog 名称当作实际发声保证。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：Bank／Program 列表优先展示现有 Catalog resolver 解析出的有效音色表；名称缺失仍可输入任意合法数值，不限制只能选“已知音色”。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### 本组不重新开放的正确性边界

- 实际命中的非音符 target 沿用后来编辑者覆盖；未命中的导入重复保留；成员修改／包装失效同一事务完成。
- List 合并包装行后，同一成员不能重复计入选择；区分“一处乐器变化”与原始 MIDI 消息数量。
- Split、owner 转换／删除、复制依赖重映射不得留下悬空关联；音乐内容转换沿用已批准的共同字段／丢弃确认合同。
- 无 SoundFont 仍允许编辑，只禁试听；不得偷偷抢停正常 Project 播放。Master／Limiter、预载、配置冻结、原生错误、关闭释放与编辑优先是必须满足的既有要求。
- “Bank A → PC P → Bank B（没有下一条 PC）”不能机械拼三个末值作为已采用音色；跨 Track 查询遵循正式顺序，不让 UI 重新解释音频。

## 4. Lane Tabs、友好数值与阶梯线

关联 R12、R24、R26、R28、R29；与 D-STATE03.a 的所有权表一起阅读。

### D-LANE01.a — 关闭 Tab 的含义

**具体问题：** 关闭一个包含事件的 Lane Tab，应仅隐藏该 Tab、保留数据并允许重新打开，还是只有空 Tab 可以关闭，或完全不提供关闭？需要将“隐藏视图”和“删除事件／Mapping owner”分成不同操作吗？

**推荐方案：** Close 仅隐藏 Tab，不删除事件、参数或 Mapping owner；可以重新打开。删除数据／删除 owner 是独立命令。

**分歧／代价：** 或者仅空 Tab 可关闭；或者全部不允许关闭。

**用户回答：**

同意 "删除数据／删除 owner 是独立命令" 这一点，但其余问题需要进一步讨论。

如果允许隐藏 Tab，视图更灵活，但会出现“我刚创建了一些事件，为了省空间我关闭了Tab，过了一会，我忘了刚才创建的事件是哪个了”的情况；

如果只有空 Tab 可以关闭，能避免上述情况，但大概率会让 Tab 非常多（尤其导入的 MIDI），考虑到事件的 Tab Header 的名称一般都不短，所以会导致要在头部翻好久。

怎么办？

我想到的是允许隐藏 Tab + 提供一个新入口/界面来展示包含事件的事件名列表，并显示事件个数，点击事件名可打开对应 Tab。
不过，我担心这会导致另一个对事件很重的O(N)遍历，导致性能倒退。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：Q2 D-LANE01.g 已确认：普通 target Tab 可隐藏，关闭不删数据／owner；右侧下箭头显示可搜索、虚拟化的全部目标目录及精确计数、显隐，点击显示并激活。Vel.／适用 Inst. 常驻。计数摘要增量维护，缺失时后台补建并显示统计中。

**确认状态：** 已由 Q2 D-LANE01.g 完成确认；原讨论保留，非待答。

### D-LANE01.b — 基础 Tab 是否可隐藏

**具体问题：** Velocity 第一、Instrument Changes 适用时第二，这两个基础 Tab 是否也允许隐藏，还是必须始终保留？如果允许隐藏，用户应能通过重新打开入口恢复，而不必创建新数据。

**推荐方案：** Velocity 和 Instrument Changes 也可以隐藏；显示时保持 Velocity 第一、Instrument Changes 第二。全部隐藏时保留重新打开入口。

**分歧／代价：** 若希望基础 Tab 永远存在，可禁止这两项关闭。

**用户回答：** 希望基础 Tab 永远存在，禁止这两项关闭。另外，将 Velocity 的 Tab 头文字写成"Vel."； Instrument Changes 的 Tab 头文字写成 "Inst."，以节约横向空间。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：基础 Tab 常驻，不允许关闭／隐藏；第一项标题 Vel.，适用时第二项 Inst.。Logical Segment 不因此新增 Inst.。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-LANE01.c — 自动显示与激活时机

**具体问题：** 用户主动隐藏某个 Lane 后，什么情况下应再次显示并激活它：仅显式 Add／Locate 等导航操作，还是数据刷新、Undo 或发现该 Lane 仍有数据时也自动恢复？

**推荐方案：** 显式 Add、Locate 会显示并激活目标 Tab；普通刷新、后台任务、Undo 不自动把隐藏 Tab 抢回来。普通属性编辑也不无故切换当前 Tab。

**分歧／代价：** “只要有数据就自动显示”会与手动隐藏冲突，不推荐。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：显式 Add、Locate 会显示并激活目标 Tab；普通刷新、后台任务、Undo 不自动把隐藏 Tab 抢回来。普通属性编辑也不无故切换当前 Tab。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-LANE01.d — 普通 targets 的初始顺序

**具体问题：** 导入或首次打开时存在多个普通 target，Tab 应按稳定的 target 顺序初始化，之后新建的追加到末尾并保留用户重排，还是每次内容变化都重新按 target 排序？

**推荐方案：** 导入／首次发现的普通 targets 按确定的 target 顺序排列；用户新建追加到普通 Tab 末尾；用户重排后保留顺序。

**分歧／代价：** 每次重新排序会让手动排序失效。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：导入／首次发现的普通 targets 按确定的 target 顺序排列；用户新建追加到普通 Tab 末尾；用户重排后保留顺序。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-LANE01.e — 换 Lane 与既有选择

**具体问题：** 已选中 CC11 等 Lane 的点后切换到另一个 Lane，旧点的选择状态应保留，还是自动清空？这会决定切回时是否仍选中，以及跨类型操作菜单面对的是哪些对象。

**推荐方案：** 切换 Lane 不偷偷清除旧 Lane 的选择。存在跨类型选择时，继续按明确的类型子菜单操作。

**分歧／代价：** 可以改为切 Lane 清空旧点选择，但这是一次选择行为变更。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：切换 Lane 不偷偷清除旧 Lane 的选择。存在跨类型选择时，继续按明确的类型子菜单操作。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-LANE01.f — Snap 与轴同步边界

**具体问题：** Lane 改为 Tabs 后，钢琴卷帘和底部事件视图是否仍保留各自独立的 Snap 开关／值，事件 Tabs 共用事件 Snap、各自记忆纵轴并共享水平位置？还是要进一步将钢琴卷帘与事件 Snap 合并联动？

**推荐方案：** 保留钢琴卷帘 Snap 与底部事件 Snap 两套独立设置；底部数值事件 targets 共用事件 Snap。不同 Lane 的纵轴范围独立，水平时间轴同步。

**分歧／代价：** 不建议因为工具栏合并而把两套 Snap 合成一套。不适用于当前 Lane 的工具不得假装可用。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：保留钢琴卷帘 Snap 与底部事件 Snap 两套独立设置；底部数值事件 targets 共用事件 Snap。不同 Lane 的纵轴范围独立，水平时间轴同步。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VAL01.a — 友好 CC 显示白名单

**具体问题：** 将部分 CC 的原始 0～127 显示成 −64～63 等友好数值时，首批是否只转换 CC10、CC74？若还要转换其他 CC，请指定 CC 号及显示范围／换算方式，以免猜测其含义。

**推荐方案：** 首批限定 CC10、CC74，显示值为 raw − 64，即 −64～63。

**分歧／代价：** 如还有其他 CC，请补充具体编号及转换，不把“等等”当成任意 CC 都减 64。CC74 的显示 0 不意味着所有设备共同的物理 Hz／声学中性值。

**用户回答：**

使用[-64, 63]的事件：

CC 10、71、72、73、74、75、76、77、78。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：友好值白名单为 CC10、CC71～78，显示范围 −64..63，换算 display=raw−64；底层 MIDI 值仍为 0..127。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VAL01.b — 友好数值覆盖入口与表达式边界

**具体问题：** 例如 CC10 的界面中心值显示为 0、底层仍为 64：属性、列表和坐标是否统一显示友好值，但 Batch Edit 表达式及既有预设仍按原始值计算？还是连表达式也改成显示域，并接受旧预设结果可能改变？

**推荐方案：** 图形纵轴、坐标、提示、List、Properties、适用 Initial State 使用友好值；既有表达式、Batch、生成器、Mapping、预设仍使用各上下文的正式数值，标签／Help 明确说明。

**分歧／代价：** 表达式也改用显示值会改变旧表达式和预设结果，不推荐。Pitch Bend 的 Direct／SubVoice 编码差异要独立适配，不能直接套 CC 偏移。

**用户回答：**

表达式也改用显示值，无需考虑兼容性。

现在有 0 个用户正在使用软件，有 0 个项目使用了表达式，有 0 个真实使用的预设。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：Q2 D-VAL01.d 已将“表达式也改”明确限定为编辑显示与数值编辑工具。目标 CC 的 Batch／生成器等工具使用显示域，Project Mapping Function、内置 Step、共享 accumulator 和原始 MIDI 数据仍保留 raw 音乐语义；Help 必须解释两层区别。不保留旧工具表达式／预设结果兼容要求，不代表放弃旧项目音乐读取。

**确认状态：** 已确认编辑工具显示域与数据／Mapping 原契约分离，详见 D-VAL01.d。

### D-VAL01.c — 原始值是否另设显示模式

**具体问题：** 采用友好显示后，原始 MIDI 值只需在 Tooltip／辅助信息中可查，还是需要另加一个可将整个界面切回原始值的显示模式？

**推荐方案：** raw 数值放在 tooltip 或辅助信息里，不新增全局 raw／display 切换模式。

**分歧／代价：** 全局切换可做，但增加组合测试与用户理解成本。

**用户回答：** 完全不显示原始 MIDI 值。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：上述友好值场景不额外展示 raw 值，包括 Tooltip；不是禁止 Program／Bank 等其他字段正常显示其数值。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STEP01 — 哪些 target 画阶梯线

**具体问题：** 阶梯线是否仅用于适合“保持当前值”语义的普通 CC／逻辑参数等数值 target，而不把 Velocity、Bank／Program、命令事件、opaque 事件全部一律连线？是否有其他明确需要纳入或排除的 target？

**推荐方案：** 阶梯线用于状态型标量事件及逻辑参数。Velocity 仍为柱；Instrument Changes 仍为固定 y 点；opaque、Bank／PC、CC120～127 等命令／特殊目标不机械套数值保持线。Value Curve／Envelope 不改。

**分歧／代价：** 如希望特殊 target 也画线，需要逐个指定它表示什么。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：阶梯线用于状态型标量事件及逻辑参数。Velocity 仍为柱；Instrument Changes 仍为固定 y 点；opaque、Bank／PC、CC120～127 等命令／特殊目标不机械套数值保持线。Value Curve／Envelope 不改。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STEP02 — 无前驱及跨轨道状态

**具体问题：** 当前 Lane 在可见范围之前没有自己的显式点时，线应从它的第一个显式点开始，还是需要从 Initial State 或同 Channel 其他轨道还原状态后开始绘线？也就是这条线表示本 Lane 数据，还是整个 Channel 的最终有效状态？

**推荐方案：** 阶梯线表达当前 Lane 自身记录的值变化。可视区左边有同 owner 的前驱点则接入；完全没有前驱时从首个显式点开始，不凭默认值或其他轨道状态虚构一条线。

**分歧／代价：** 若希望显示 Channel 真正有效状态，需要跨轨道／Initial State 查询及来源区分，范围更大。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：阶梯线表达当前 Lane 自身记录的值变化。可视区左边有同 owner 的前驱点则接入；完全没有前驱时从首个显式点开始，不凭默认值或其他轨道状态虚构一条线。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STEP03 — Segment 界外内容与线的语义

**具体问题：** Segment 暴露范围外仍保留可编辑的事件点时，是否继续显示它们的阶梯线并用界外暗区明确区分，而不让线被误解为范围外仍参与发声的状态？还是只在 Segment 暴露范围内绘线？

**推荐方案：** Segment 界外既有点仍可展示／编辑；界外线只是数据可视化，不表示该区间实际发声。可听区间与界外区间有明确视觉区分，不跨不相关 owner 接线；SubVoice 按模板边界处理。

**分歧／代价：** 不建议将界外数据一律隐藏来简化画线。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：Segment 界外既有点仍可展示／编辑；界外线只是数据可视化，不表示该区间实际发声。可听区间与界外区间有明确视觉区分，不跨不相关 owner 接线；SubVoice 按模板边界处理。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STEP04 — 线命中与显示开关

**具体问题：** 阶梯线本身应仅作显示、操作仍命中事件点，还是点击线段也能编辑对应状态？阶梯线是否默认显示并提供关闭选项？

**推荐方案：** 线仅辅助展示，不参与命中；仍拖点编辑。默认显示线，并提供关闭线的视图选项。

**分歧／代价：** 若允许点击线选择或修改前驱点，需要增加新的交互优先级。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：线仅辅助展示，不参与命中；仍拖点编辑。默认显示线，并提供关闭线的视图选项。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

## 5. 连续跟随与播放琴键染色

R10 连续跟随经 Q2 扩为两类 Segment、All Tracks、Arrangement、Conductor；R11 琴键染色仍限两类 Segment 与 All Tracks，不扩大到 SubVoice。行为与衔接已确认，性能结论仍必须实测。

### D-VIS02.a — 跟随模式默认值

**具体问题：** Follow 总开关仍默认关闭；用户主动开启后，默认应使用原有的跳跃跟随，还是新增的连续跟随？这里选择的是跟随模式默认值，不是让 Follow 默认开启。

**推荐方案：** 保留原跳跃跟随为默认，新增连续跟随可选。Follow 总开关仍默认关闭。

**分歧／代价：** 若希望以后开启 Follow 就默认连续，可将连续设为新默认模式；不得混淆“模式默认”与“总开关默认”。

**用户回答：**

同意。程序顶部现有的 Follow 开关按钮，要做改动：

默认关闭；点击第一下 = 跳跃跟随；点击第二下 = 连续跟随；点击第三下 = 关闭跟随。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：顶部 Follow 按关闭→跳跃→连续→关闭循环，默认关闭。Q2 D-VIS02.f 将连续范围明确扩大至两类 Segment、All Tracks、Arrangement、Conductor；其他原本支持 Follow 而尚未实现连续的视图沿用跳跃，并说明当前实际模式。未扩大 R11 琴键染色范围。

**确认状态：** 已确认三态、范围扩展及其他视图衔接，详见 D-VIS02.f。

### D-VIS02.b — 连续模式锚点

**具体问题：** 连续跟随时，播放指针在视图内的固定位置应允许按 0～100% 自定义并默认 50%，还是只提供左侧／居中等少量预设？该锚点是否作为程序级偏好供相关视图统一使用？

**推荐方案：** 锚点可设为视图宽度的 0～100%，默认 50%，提供靠左／居中快捷值；模式和锚点作为程序偏好。

**分歧／代价：** 仅提供靠左／居中更简单，但限制原要求的可配置位置。

**用户回答：** 同意。不过，锚点默认最左侧。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：连续锚点允许 0～100% 配置，默认最左侧 0%，不是 50%；作为程序级偏好。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VIS02.c — Tick 0 左侧空白

**具体问题：** 从 Tick 0 开始播放、又要求播放指针固定在视图中间时，是否允许左侧出现 Tick 0 之前的空白？还是应先让指针从左边走到锚点，再开始固定位置连续滚动？

**推荐方案：** 不显示负 Tick。播放初期不足以把指针放到锚点时，先让指针向锚点走，之后固定。

**分歧／代价：** 或允许 Tick 0 左侧出现空白，让指针从一开始就位于锚点。

**用户回答：** 同意。负 Tick 容易出事故。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：不显示负 Tick。播放初期不足以把指针放到锚点时，先让指针向锚点走，之后固定。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VIS02.d — 手动平移与 Follow

**具体问题：** Follow 开启期间用户手动平移视图，应明确关闭 Follow 并取消按钮高亮，还是只临时暂停跟随、按钮继续亮着？若选临时暂停，需要在什么操作或时机恢复？

**推荐方案：** 用户主动平移时明确关闭 Follow，按钮同步关闭；再次开启恢复。

**分歧／代价：** 或临时暂停但按钮仍启用，这种状态不够直观。

**用户回答：** 只临时暂停跟随、按钮继续亮着。保持现有的行为，别改。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：保留现有手动平移时临时暂停、按钮仍亮的行为；中键／概览拖动结束或失去捕获后恢复跟随，不改成关闭开关。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VIS02.e — 手动缩放与 Follow

**具体问题：** Follow 开启期间用户手动水平缩放，是否继续保持跟随及所设锚点，还是和手动平移一样关闭／暂停 Follow？

**推荐方案：** 用户缩放时保持 Follow，以播放指针锚点处理缩放；不把缩放也当成退出跟随。

**分歧／代价：** 也可以所有手动视图操作都关闭 Follow。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：用户缩放时保持 Follow，以播放指针锚点处理缩放；不把缩放也当成退出跟随。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VIS01.a — 琴键染色的音符语义

**具体问题：** 播放时给琴键染色，应跟随当前视图所显示的音符与 Gate，还是跟随最终编译的实际音高？例如逻辑音符 Key 60 经 Mapping 变为 Key 72 时，在原始音符视图中应亮 60 还是 72？

**推荐方案：** 按当前视图显示的音符 Gate 判断：原始逻辑视图按原始逻辑音符，Compiled 按该模式显示的音符。不是 BASS 真实 voice／原生 release 的监视器。

**分歧／代价：** 若要求最终发声状态，需要正式编译／运行状态投影，性质和成本不同。

**用户回答：** 同意。不过，更激进的优化方式：直接读当前播放指针下1px音符图像如何？这样就不用算每个音符的gate了，直接类似O(k=127)复杂度。不过，经过音符边框时颜色会错。能读不带边框的图像吗？

**补充问答：** 2026-09-14 归纳（非改写用户原答）：琴键染色遵循当前显示音符的 Gate，而非后端 voice；用户另提的逐像素读取是待评估技术建议，不是已批准改成近似音符状态。技术解释见决策汇总 §3。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VIS01.b — 染色是否过滤 Mute／Solo

**具体问题：** 当前视图／洋葱皮中的音符因 Mute／Solo 被静音时，是否仍按可见音符给琴键染色，还是仅对实际允许发声的轨道染色？

**推荐方案：** 不按 Mute／Solo 过滤染色，与当前可见音符一致。

**分歧／代价：** 可只高亮应发声轨道，但琴键色与可见洋葱皮将不再完全一致。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：不按 Mute／Solo 过滤染色，与当前可见音符一致。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VIS01.c — 同 key 多来源颜色

**具体问题：** 同一时刻同一个 Key 有多个不同颜色来源时，琴键应优先采用当前轨道颜色，其次采用最上层来源的颜色，还是需要混色／分条显示多个来源？

**推荐方案：** 当前编辑轨道优先，其余按洋葱皮叠放顺序取最上层颜色；All Tracks 按后面的轨道优先。

**分歧／代价：** 混色或条带能表达多来源，但密集时更杂乱。

**用户回答：** 同意。不过，取决于 D-VIS01.a 的讨论结果，如果可以用其激进优化方式，这个问题就不重要了。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：当前轨道优先，其余取最上层可见来源；不因像素读取建议取消多来源颜色优先级。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VIS01.d — 染色开关与播放状态

**具体问题：** 播放琴键染色应默认开启并可关闭，还是默认关闭？Buffering 时冻结在当前播放位置、停止时清除、跳转时重新计算，这套状态行为是否符合预期？

**推荐方案：** 染色可关闭；性能通过后默认开启。Buffering 时停在当前播放 Tick 的键色，Stop 清空，Seek／Loop 按新 Tick 更新。

**分歧／代价：** 可以默认关闭，让用户主动启用。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：染色可关闭；性能通过后默认开启。Buffering 时停在当前播放 Tick 的键色，Stop 清空，Seek／Loop 按新 Tick 更新。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-VIS03 — 性能不达标的退让政策

**具体问题：** 连续跟随或琴键染色在大型 MIDI 上若无法通过现有流畅度基线，应暂缓该功能，还是允许降低装饰更新频率等明确的视觉退让？哪些退让可接受，而不能让它拖慢编辑、播放或核心渲染？

**推荐方案：** 连续跟随／琴键色未通过性能门时，不以牺牲现有编辑和播放流畅性换取上线。先优化；仍不合格则暂缓对应效果，不在运行中偷偷切换另一种显示语义。

**分歧／代价：** 可批准降低装饰效果刷新率，但不能降低音乐数据或时间定位的正确性。当前不编造收益／退化数值。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：连续跟随／琴键色未通过性能门时，不以牺牲现有编辑和播放流畅性换取上线。先优化；仍不合格则暂缓对应效果，不在运行中偷偷切换另一种显示语义。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

## 6. Segment Tracks 侧栏与文件打开

关联 R30、R04；外部新窗口行为需与 R01 多实例交付协调。

### D-TRACK01.a — 侧栏默认与轨道头点击

**具体问题：** 两种 Segment 视图新增的 Tracks 侧栏是否默认隐藏？其中单击轨道头应只选择／取消选择，双击才跳到该轨道的 Segment，还是希望单击就立即跳转？

**推荐方案：** 两类 Segment 的 Tracks 侧栏默认隐藏。单击轨道头沿用 Arrangement 的选择／取消选择行为，不立即跳 Tab；双击进入目标轨道的 Segment。

**分歧／代价：** 单击即跳转更快，但会干扰轨道选择及管理操作。

**用户回答：** 同意。此外，请高亮“当前Segment所在轨道”。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：Tracks 侧栏默认隐藏；单击选择／取消选择，双击导航。另以独立视觉标记高亮当前 Segment 所属轨道，不能与操作选中态混淆。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-TRACK01.b — 双击 Track 的定位规则

**具体问题：** 双击目标轨道时，如果编辑指针所在位置没有 Segment、而该轨道有多个 Segment，应打开距离最近的一块，还是弹出选择器／不跳转？等距时优先前一块、空轨道不自动创建内容，是否合适？

**推荐方案：** 优先打开蓝色编辑指针所在的 Segment；指针在空隙时打开最近的 Segment，等距取前一个；空轨道不自动创建 Segment。

**分歧／代价：** 可改为弹出 Segment 列表，或空隙时不跳转。

**用户回答：** 同意。如果轨道完全没有 Segment，则什么也不做。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：优先打开包含编辑指针的 Segment，否则最近者、等距取前者；轨道无 Segment 时什么也不做、不自动创建。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-TRACK01.c — 轨道头操作覆盖面

**具体问题：** Tracks 侧栏应复制 Arrangement 轨道头的哪些管理能力？是否包含属性、复制／剪切／粘贴／拷贝、绑定／共享、排序和 Mute／Solo 等全部适用操作，只排除内容区编辑；还有哪些需要明确新增或排除？

**推荐方案：** 复用 Arrangement 的改名、属性、颜色、绑定／路由、复制剪切粘贴、排序、共享组、Mute／Solo 等轨道命令；不复制右侧内容画布。删除当前 owner 时正常关闭相关 Tab。

**分歧／代价：** 如“几乎所有功能”还包含其他内容区功能，请补充具体项。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：复用 Arrangement 的改名、属性、颜色、绑定／路由、复制剪切粘贴、排序、共享组、Mute／Solo 等轨道命令；不复制右侧内容画布。删除当前 owner 时正常关闭相关 Tab。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-TRACK01.d — 轨道操作目标与音符选择

**具体问题：** Tracks 侧栏与 Arrangement 是否共用同一个“当前选中轨道”操作目标，还是各自独立？在侧栏单击轨道头时，是否保留钢琴卷帘中已有的音符／事件选择，不将其一并清空？

**推荐方案：** 侧栏与 Arrangement 共用当前轨道操作目标，但轨道头操作不无故清除钢琴卷帘音符选择。

**分歧／代价：** 独立轨道头选择会出现两个不同的“当前轨道”，不推荐。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：侧栏与 Arrangement 共用当前轨道操作目标，但轨道头操作不无故清除钢琴卷帘音符选择。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-OPEN01.a — 扩展名与一次多个文件

**具体问题：** 文件打开入口除了 `.mid` 是否也接受 `.midi`？一次拖入或通过外部命令传入多个文件时，应明确拒绝这次多文件请求，还是为每个文件分别新建窗口，而不能静默只打开第一个？

**推荐方案：** 接受 .midora、.mid、.midi；每次请求一个文件。一次拖入多个文件明确提示，不静默只取第一个。

**分歧／代价：** 多个文件分别开新实例也可以，但依赖多实例专题完成。

**用户回答：** 同意。多个文件不要分别开新实例，不然误操作代价过大。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：接受 .midora／.mid／.midi；一次只处理一个文件，多文件整次明确拒绝，不分别新开实例、不静默只取首个。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-OPEN01.b — 目标窗口规则

**具体问题：** 菜单／拖入当前窗口的打开操作是否继续替换当前 Project，而系统“打开方式”／命令行打开文件则创建新窗口？若同一文件已经打开，是否应定位已有窗口，避免再建可写会话？

**推荐方案：** 拖入当前窗口、窗口内 Open／New：替换该窗口当前项目，保留未保存确认。多实例完成后，系统 Open With／命令行带文件默认开新窗口；同文件已有可写实例时定位它。

**分歧／代价：** 也可以所有外部打开优先替换已有窗口。多实例尚未实施时，不提前破坏当前单实例边界。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：拖入当前窗口、窗口内 Open／New：替换该窗口当前项目，保留未保存确认。多实例完成后，系统 Open With／命令行带文件默认开新窗口；同文件已有可写实例时定位它。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-OPEN01.c — 模态与长任务中的打开请求

**具体问题：** 窗口正在显示模态弹窗或执行长任务时收到新的文件打开请求，应立即明确告知当前不能处理，还是排队等空闲后打开？后者可能在用户已开始其他操作后才切换项目。

**推荐方案：** 模态编辑或前台长任务期间，新打开请求明确提示暂不能处理，不偷偷排入很久之后才执行的队列。取消／导入失败保留原项目。

**分歧／代价：** 排队可做，但迟到打开容易让用户误以为程序自发切项目。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：模态编辑或前台长任务期间，新打开请求明确提示暂不能处理，不偷偷排入很久之后才执行的队列。取消／导入失败保留原项目。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-OPEN01.d — 文件关联

**具体问题：** 本批只需支持用户通过系统“打开方式”选择 Midora.exe 打开文件，还是还需要程序内提供显式注册文件关联的入口？不应未经用户操作自动修改系统默认应用。

**推荐方案：** 支持用户在 Windows 打开方式里手选 exe；不自动注册默认关联、不修改默认应用。

**分歧／代价：** 如需程序内“注册文件关联”，在此明确批准附加范围。

**用户回答：** 同意。不自动注册默认关联、不修改默认应用，尽量保持最小系统级侵入，选择权尽量交给用户。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：支持用户自行用系统 Open With 选择 Midora.exe；不自动注册默认关联、不改变默认应用或增加无必要系统写入。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

## 7. 完整工作区恢复及同轨道共享设置

关联 R06、R07，以及 R28、R30 的视图状态。下文表 A/B 已获 Q1 批准；存活对象的 Onion 来源引用和工具字段所有权已由 Q2 D-STATE03.d/e 补齐并确认，尚未同步正式 SRS／实现。

### D-STATE01.a — 完整保存白名单

**具体问题：** “完整恢复工作区”是否采用下方附表 A 的全部轻量状态，包括已关闭对象的视图设置，但不保存大选区、Undo／Redo、弹窗草稿、运行任务及缓存？请指出白名单中仍应增加或删除的类别。

**推荐方案：** 采用本节附表 A 的全部轻量状态；包含已关闭对象视图的轻量状态，但不保存大选区、Undo／Redo、模态草稿、正在运行的任务、播放／按键保持状态、编译结果或图形／音频缓存。

**分歧／代价：** 可删掉不值得保存的类别；若要求全量选区，必须另外评估百万对象保存成本，不能默默包含。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：采用本节附表 A 的全部轻量状态；包含已关闭对象视图的轻量状态，但不保存大选区、Undo／Redo、模态草稿、正在运行的任务、播放／按键保持状态、编译结果或图形／音频缓存。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STATE01.b — 恢复字段与 Stopped 启动

**具体问题：** 重开项目时，工具模式、指针位置和诊断筛选等编辑状态应恢复到保存时的值，还是其中一些应每次重置？播放运行状态是否始终恢复为 Stopped、越界位置按有效范围处理，而不自动续播？

**推荐方案：** 附表 A 全纳入。重开永远 Stopped；指针超出合法范围时修正到合法位置，绝不自动续播。

**分歧／代价：** 可单独排除当前工具模式、两个指针或诊断筛选等类别；不把完整恢复等同于恢复正在运行的任务。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：附表 A 全纳入。重开永远 Stopped；指针超出合法范围时修正到合法位置，绝不自动续播。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STATE01.c — 组级 Mute／Solo

**具体问题：** 保存 Mute／Solo 时，是否连共享组本身的状态也与各成员轨道的状态分别保存和恢复，还是只保存轨道级状态？组状态仍不能改写成员状态，也不能影响 MIDI／音频文件导出的正式内容。

**推荐方案：** Track 和共享 Usage／Root 组 Mute／Solo 一起保存，恢复后首次播放按其过滤；两级状态相互独立。

**分歧／代价：** 只保存 Track 无法完整恢复组级监听状态。恢复的只是运行期初值，不改 canonical、MIDI 或音频文件成品。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：Track 和共享 Usage／Root 组 Mute／Solo 一起保存，恢复后首次播放按其过滤；两级状态相互独立。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STATE02.a — 显式保存与 Modified

**具体问题：** 只有视图／Mute／Solo 状态改变时，是否接受不出现未保存星号、不触发关闭保存提醒，但用户显式 Save 时仍写入这些状态？这意味着关闭前未手动保存的视图调整可能不会保留。

**推荐方案：** 仅显式 Save／Save Copy 保存视图；没有音乐修改也能 Save 视图。不加 \*、不进入 Undo，关闭时不因纯视图变化提示保存。

**分歧／代价：** 未显式保存的视图变化会丢失；不顺带引入自动保存。Save Copy 沿用既有保存／迁移语义，不冒充普通 Save。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：仅显式 Save／Save Copy 保存视图；没有音乐修改也能 Save 视图。不加 \*、不进入 Undo，关闭时不因纯视图变化提示保存。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STATE02.b — 损坏隔离与懒恢复

**具体问题：** 项目音乐内容正常、但某一块视图状态损坏时，是否只恢复其他有效部分并让损坏部分回到默认值？重开大量 Tabs 时，是否只立即建立活动 Tab，其余等切换过去再加载，而不是全部同时初始化？

**推荐方案：** 分区独立恢复：某个视图区坏了，只回退该区并 Warning，音乐仍加载。隐藏 Tabs 懒加载，不为恢复后台 Tab 提前编译／渲染；活动 Compiled 按正常流程后台准备。

**分歧／代价：** 全成功或全失败更简单，但一个字段损坏就丢所有布局，不推荐。没有准备完的内容不能伪称已经完成。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：分区独立恢复：某个视图区坏了，只回退该区并 Warning，音乐仍加载。隐藏 Tabs 懒加载，不为恢复后台 Tab 提前编译／渲染；活动 Compiled 按正常流程后台准备。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STATE02.c — 视图数据超限

**具体问题：** 如果需要保存的视图状态超过安全资源上限，应允许用户确认后保存音乐内容及可保留的视图部分，还是必须让本次保存失败／取消？不能静默截断，也不能因为视图数据让音乐内容无法读取。

**推荐方案：** 设明确资源上限；意外超限时提示，并允许保存音乐及可用视图部分，或取消。不静默裁剪，也不让视图问题永久阻止音乐保存。

**分歧／代价：** 具体字节／条目／恢复并发预算由基准测试确定，不要求用户现在猜数值。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：设明确资源上限；意外超限时提示，并允许保存音乐及可用视图部分，或取消。不静默裁剪，也不让视图问题永久阻止音乐保存。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STATE02.d — 旧软件重写未知视图字段

**具体问题：** 将新版本保存的项目交给仍能读取其音乐内容的旧版本时，旧版本再次保存可能丢失它不认识的新视图字段。是否接受这一限制，还是要求旧版本也能原样保留未知视图字段？

**推荐方案：** 接受旧软件重写项目时可能丢失它不认识的新增 presentation 字段，但不能损坏音乐；新软件保持旧音乐格式读取能力。

**分歧／代价：** 要求任意旧软件无损保留未知字段，需要额外兼容机制。不得以此静默改变 strict schema 或冻结的旧格式。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：接受旧软件重写项目时可能丢失它不认识的新增 presentation 字段，但不能损坏音乐；新软件保持旧音乐格式读取能力。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STATE03.a — 同 Track 通用 profile 的所有权

**具体问题：** 同一轨道下的多个 Segment，应共享哪些视图设置、各自保留哪些？例如修改水平缩放／Snap 可同步到同轨道其他 Segment，但滚动位置、当前 Lane 和各 Lane 纵轴独立；是否按下方附表 B 划分，还是需要调整具体字段？

**推荐方案：** 采用本节附表 B：同 Track 实时共享通用 profile；不同 Track 不再因全局编辑设置被连带修改；共享 Usage／Root 不意味着共享视图 profile。Lane 的纵轴／排列按 Segment 独立。

**分歧／代价：** 如仍希望某些字段全项目共用，或 Lane 纵轴／排列也同 Track 实时联动，请逐字段指定。真实 Lane 存在性不能由 profile 在另一 Segment 创建。

**用户回答：** 同意。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：采用附表 B：同 Track 实时共享通用 profile，不同 Track 隔离，Usage／Root 共享不意味着视图共享。Lane 纵轴／排列按 Segment 独立。Q2 D-STATE03.e 补齐当前工具字段，按 D-UI01.d 最终定义保存共享主 ToolMode 和独立事件绘线形态，不拆成两套主工具。

**确认状态：** 已确认附表 B 及 Q2 工具字段补充；无未答的所有权问题。

### D-STATE03.b — 显式打开与恢复位置的优先级

**具体问题：** 重新打开一个以前看过的 Segment 时，若它记住了旧视图位置、当前编辑指针又位于该 Segment 内，应优先恢复旧位置，还是优先将编辑指针居中？普通 Tab 切换、显式打开及项目重开是否需要区别处理？

**推荐方案：** 从 Arrangement 显式打开 Segment 且编辑指针在其中时，保留定位到编辑指针的行为；普通切 Tab、重开项目优先恢复原 viewport。

**分歧／代价：** 也可始终恢复位置，仅 Locate 命令才定位。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：从 Arrangement 显式打开 Segment 且编辑指针在其中时，保留定位到编辑指针的行为；普通切 Tab、重开项目优先恢复原 viewport。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-STATE03.c — 复制、转轨与删除的状态处理

**具体问题：** 拷贝轨道时，视图设置应复制为互不联动的新状态，还是与原轨道共用？Segment 转轨后是否使用目标轨道的共享设置；删除对象再 Undo 时，是仅保留可再次打开的轻量状态，还是连原 Tab 也自动重开？

**推荐方案：** Duplicate Track 复制轻量 profile 后各自独立；Segment 转入另一 Track 后使用目标通用 profile，保留合法的自身局部位置。删除 owner 后可短期保留状态供 Undo 恢复，不自动重开 Tab。

**分歧／代价：** 不建议复制后两个 Track 的视图设置仍互相联动；短期保留要有明确释放边界，不能无限累计孤立状态。

**用户回答：** 同意。不过，不要“删除 owner 后短期保留视图状态”，这个释放时机把控不好会出大事。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：Duplicate Track 复制轻量 profile 后独立；转轨使用目标 profile 并保留合法局部位置。删除 owner 立即释放其自身状态，Undo 不自动重开 Tab。Q2 D-STATE03.d 允许存活 A 保留已删除 B 的受预算 dormant 来源 ID，但不持有 B 的对象／页／缓存／任务／lease；保存过滤悬空，关项目释放。

**确认状态：** 已确认自身资源释放与存活对象轻量引用分离，详见 D-STATE03.d。

### 附表 A — D-STATE01.a 的完整保存白名单（Q1 原表，已确认）

| 类别                  | 已确认保存内容                                                                 |
| --------------------- | ------------------------------------------------------------------------------ |
| Workspace             | 打开的 Tabs、顺序、活动 Tab；已关闭对象视图的轻量状态，以便再次打开            |
| Arrangement／钢琴卷帘 | 缩放、滚动位置、当前工具、Grid／Snap／单位、适用默认音长／力度、面板显隐和尺寸 |
| Lane Tabs             | 显隐、顺序、活动 target、各 target 纵轴状态                                    |
| Event Instrument      | 当前内部 Tab／SubVoice、左栏宽度／滚动／折叠、适用键盘显示状态                 |
| 对象列表／Tracks      | 宽度、列宽、筛选／排序等轻量展示状态                                           |
| Onion／All Tracks     | 已确认的来源、快捷模式、透明度；All Tracks 模式和 viewport                     |
| 指针／监听            | 静止 Edit／Playback Cursor；Track 与 Usage／Root 组独立 Mute／Solo             |
| 其他                  | Diagnostics 筛选、Project Settings 当前页等轻量导航                            |

排除大 Selection、Undo／Redo、模态草稿、正在运行的任务、播放／按键保持状态、编译结果和图形／音频缓存。不存在／已删除引用按经批准的恢复规则处理，不能产生第二套正式对象身份。

### 附表 B — D-STATE03.a 的状态所有权（Q1 已确认，Q2 补齐工具字段）

| 所有者        | 状态                                                                                                                    |
| ------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Track 共用    | Piano 水平／垂直缩放、Grid；Piano Snap 与事件 Snap 两套独立配置；共享主 ToolMode＋事件绘线形态；默认音长／力度；Lanes／List／Tracks 显隐及通用面板尺寸 |
| Segment 自己  | 滚动起点、活动 Lane、实际 Lane 显隐／顺序、各 target 纵轴、列表局部导航状态                                             |
| SubVoice 自己 | 自己的上述编辑状态，不因 Logical Track profile 改变                                                                     |
| 项目视图      | Arrangement、All Tracks、Tabs 布局等各自状态                                                                            |

重点：Lane 的纵轴和排列按 Segment 独立，因为同 Track 各 Segment 的事件种类可能不同。profile 不能在其他 Segment 隐式创建 Lane、事件或 Mapping owner。Q2 D-STATE03.e 已补齐工具字段；主区和 Lane 不拆两套主 ToolMode。普通 Tab 隐藏入口及目录已由 D-LANE01.g 确认，保存显隐不等于删除数据。

## 8. 多实例与跨项目剪贴板

关联 R01；同时对 R04 外部打开、R06 保存协调有影响。首个切片与本批最终范围分开批准，不能用第一切片代表最终全部完成。

### D-MI01.a — 同文件可写会话

**具体问题：** 多实例启用后，同一个 `.midora` 文件是否仍只允许一个可写会话，重复打开时定位原窗口？还是还需要第二窗口只读，或允许两个窗口同时编辑并保存同一文件？

**推荐方案：** 一个进程一个 Project；同一项目文件只允许一个可写会话。再次打开定位已有实例；要独立编辑应先另存副本。

**分歧／代价：** 第二实例只读可以增加；不推荐两个实例直接写同一文件。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：一个进程一个 Project；同一项目文件只允许一个可写会话。再次打开定位已有实例；要独立编辑应先另存副本。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI01.b — 外部修改冲突

**具体问题：** 项目打开后，磁盘上的文件又被其他程序改动，此时 Save 应阻止直接覆盖并引导重新确认／另存副本，还是允许直接覆盖外部修改？需要避免把新的磁盘内容当作原来那份文件无提示替换。

**推荐方案：** 保存时检测外部修改冲突，不能直接覆盖；允许取消、Save Copy，或明确确认后先备份磁盘当前版本再覆盖。

**分歧／代价：** 限制 Midora 多实例不能防止外部程序／同步工具修改。具体检测与原子替换必须作为一个保存安全设计，不能只改提示。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：保存时检测外部修改冲突，不能直接覆盖；允许取消、Save Copy，或明确确认后先备份磁盘当前版本再覆盖。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI01.c — 启动与 New Window

**具体问题：** 再次直接启动 Midora.exe 时，应新建空窗口还是唤起原窗口？是否同时提供明确的 New Window，并让原窗口内的 New／Open 仍按当前项目替换流程工作？

**推荐方案：** 无参数启动 exe 新建空窗口；提供 New Window。窗口内 New／Open 仍替换本窗口，不全部变成新开窗口。

**分歧／代价：** New Project 总开新窗口会改变现有工作流。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：无参数启动 exe 新建空窗口；提供 New Window。窗口内 New／Open 仍替换本窗口，不全部变成新开窗口。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI01.d — 多实例同时发声

**具体问题：** 多个 Midora 实例是否允许同时播放／试听，各自持有音频 Worker 并累加资源占用，还是全局只允许一个实例发声、启动另一个时停止前一个？

**推荐方案：** 允许多实例同时播放／试听，互不停止；资源消耗按实例累加。

**分歧／代价：** 若希望全局只有一个发声实例，需要跨实例音频所有权。各实例 Limiter 不等于 Windows 混音后的总峰值保证。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：允许多实例同时播放／试听，互不停止；资源消耗按实例累加。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI02.a — 共享配置与并发写入

**具体问题：** 同一安装目录的多个实例是否共享 SoundFont、Catalog、预设和程序设置？同时修改同一配置时，应提示冲突而不是最后保存者静默覆盖，最近项目记录则合并，还是需要实例各自独立配置？

**推荐方案：** 同 ProgramRoot 共享 Preferences、SoundFonts、Catalog、Presets、Recent。设置写冲突明确处理；Recent 合并，不由最后一个窗口覆盖全部记录。

**分歧／代价：** 每实例独立偏好需要额外 profile 概念，不推荐本批加入。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：同 ProgramRoot 共享 Preferences、SoundFonts、Catalog、Presets、Recent。设置写冲突明确处理；Recent 合并，不由最后一个窗口覆盖全部记录。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI02.b — 其他实例收到音频配置变化

**具体问题：** 实例 A 保存新的音色库／音频设置时，如果实例 B 正在播放，B 应继续使用本次冻结配置、空闲后再应用更新，还是立即中断并切换？B 已打开但未保存的设置草稿是否应保留并提示冲突？

**推荐方案：** 运行中的音频任务保持冻结配置；停止／空闲后接收新设置并按需重建 Worker。已打开的设置草稿不静默覆盖，保存前提示冲突。

**分歧／代价：** 即时强制重建其他实例 Worker 会打断播放，不推荐。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：运行中的音频任务保持冻结配置；停止／空闲后接收新设置并按需重建 Worker。已打开的设置草稿不静默覆盖，保存前提示冲突。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI02.c — 共享资源预算

**具体问题：** 例如 Audio Cache 上限设为 16GiB，多个实例应共同受这一个安装目录的 16GiB 总预算约束，还是每个实例各有 16GiB？是否接受各进程必要内存仍会累加，但共享缓存不能静默按实例数翻倍？

**推荐方案：** 同 ProgramRoot 的 reusable cache 上限按全部实例合计，不是每实例各一份。临时文件、Clipboard、后台任务也建立预算与释放机制。

**分歧／代价：** 进程内存需区分每实例预算与全局累计，不能承诺多个项目仍只有单实例内存占用。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：同 ProgramRoot 的 reusable cache 上限按全部实例合计，不是每实例各一份。临时文件、Clipboard、后台任务也建立预算与释放机制。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI02.d — 不同安装目录的边界

**具体问题：** 如果用户在两个不同目录各装一份 Midora，它们是否各自保存配置／缓存，但仍允许当前用户登录会话内跨安装目录复制粘贴？还是不同安装目录之间也应完全隔离剪贴板？

**推荐方案：** 不同 ProgramRoot 的设置／缓存独立；兼容协议下，同用户、同 Windows 登录会话的不同安装目录可以共享剪贴板。

**分歧／代价：** 不支持跨用户／远程共享；不把 Midora 自建配置或 backing 偷偷写到程序目录之外。

**用户回答：** 同意“不同 ProgramRoot 的设置／缓存独立”。但是，剪贴板应仅仅在同一安装目录下的Midora多实例间共享，因为剪贴板首先不能直接写在内存（大范围内存占用太大），也不应静默随便写磁盘在用户不知情的地方。剪贴板共享仅靠同一安装目录下的多实例共享功能专用文件夹下的文件实现，对用户透明。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：配置、缓存与音乐剪贴板均按同一 ProgramRoot 隔离；跨目录／跨 exe 共享不支持。正式剪贴板内容用程序目录下专用文件型存储，不要求全量驻留内存、不在外部路径隐式落盘。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI03.a — 本批最终对象范围

**具体问题：** 跨实例／跨项目剪贴板在本批最终完成时，应覆盖目前所有可复制对象，还是只需音符和事件？能否将音符／事件作为首个交付切片，而不是把它当作整个需求的最终范围？

**推荐方案：** 最终覆盖现有全部可复制对象类型，不仅 Note Art：音符、适用事件／参数、Segment、Track、Definition、SubVoice，以及当前已有复制入口的 Mapping／曲线等。内部先基础对象、后依赖闭包分阶段实施。

**分歧／代价：** 可收窄为本批只做音符／数值事件，其他另立未来需求；但必须明确这不是全部完成，也不暗含为原本不可复制对象新增全部复制入口。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：最终覆盖现有全部可复制对象类型，不仅 Note Art：音符、适用事件／参数、Segment、Track、Definition、SubVoice，以及当前已有复制入口的 Mapping／曲线等。内部先基础对象、后依赖闭包分阶段实施。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI03.b — 依赖闭包与目标身份

**具体问题：** 复制 Track 等带事件乐器依赖的对象到另一项目时，是否应连同所需依赖一起复制、重新分配身份，并只在这一次粘贴内部保留共享关系？目标项目已有同名乐器时，是仍创建独立副本，还是尝试自动复用？

**推荐方案：** 复制完整对象带上所需依赖闭包；目标分配新 ID，同一次粘贴内共同依赖只复制一次。不同次粘贴默认独立副本，不按名称自动绑定或覆盖目标对象。

**分歧／代价：** 按名称复用存在误绑定风险；显式复用选择器可以另作能力。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：复制完整对象带上所需依赖闭包；目标分配新 ID，同一次粘贴内共同依赖只复制一次。不同次粘贴默认独立副本，不按名称自动绑定或覆盖目标对象。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI03.c — 粘贴到已有 owner

**具体问题：** 将音符／参数等粘贴进目标已有的 Segment／事件乐器时，应沿用目标的绑定与所属对象、无法对应的依赖要求显式处理，还是自动把源绑定带过去？若想完整保留源乐器关系，是否应通过复制整轨／完整对象实现？

**推荐方案：** 粘贴进现有 Segment／Track，不自动更改目标 Usage、Root 或 Instrument；音符服从目标，参数需要明确目标映射，不能按名称猜。要保留源绑定结构，应复制完整 Track。

**分歧／代价：** 自动换绑会影响已有内容，甚至同组其他轨道；保留既有跨类型共同字段转换及丢弃确认规则。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：粘贴进现有 Segment／Track，不自动更改目标 Usage、Root 或 Instrument；音符服从目标，参数需要明确目标映射，不能按名称猜。要保留源绑定结构，应复制完整 Track。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI03.d — 跨项目 Root 路由

**具体问题：** 跨项目粘贴带 Fixed Root 的 MIDI 轨道时，应像副本一样改为 Auto、保留该次副本内部的共享和 Channel Mode，还是保留源 Port.Channel 并在冲突时询问是否与目标 Root 合并？同项目已有复制规则不因本项自动改变。

**推荐方案：** 复制 Track／Root 后保留复制子集内部共享关系与 Channel Mode，但新 Root 默认 Auto，不自动加入目标同 Port.Channel 的 Fixed Root。

**分歧／代价：** 可尽量保留 Fixed，冲突时显式选择合并／Auto／取消。本决定不顺带改变既有同项目粘贴行为。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：跨项目复制 Track／Root 后保留复制子集内部共享关系与 Channel Mode，但新 Root 默认 Auto，不自动加入目标同 Port.Channel 的 Fixed Root；不改变既有同项目粘贴行为。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI03.e — 是否保证跨项目听感一致

**具体问题：** 跨项目复制应保证对象与正式依赖按规则转换，还是还必须保证听感完全相同？两个项目的 Tempo／全局初始状态或两个实例的 SoundFont 不同时，是否接受听感差异，不为此自动冻结额外环境状态？

**推荐方案：** 不复制 SoundFont 配置和整个源项目环境；不承诺声音完全一致，尤其目标默认状态、绑定、SoundFont 不同时。

**分歧／代价：** 完整听感保持还需冻结相关有效初始状态，超出普通对象复制。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：不复制 SoundFont 配置和整个源项目环境；不承诺声音完全一致，尤其目标默认状态、绑定、SoundFont 不同时。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI03.f — Cut／Paste 的事务与 Undo

**具体问题：** 跨实例 Cut／Paste 是否作为两次独立事务：源端先可靠写剪贴板再删除、可在源端 Undo；目标粘贴可独立 Undo，失败仍可重试剪贴板？还是要求目标失败自动撤销源剪切、两端共用一次 Undo？

**推荐方案：** Cut 先可靠发布剪贴板，再在源端一条 Undo 命令删除；目标 Paste 是另一条独立命令。失败不自动撤销源 Cut，但剪贴板可重试。

**分歧／代价：** 不做跨进程共同 Undo／分布式原子移动。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：Cut 先可靠发布剪贴板，再在源端一条 Undo 命令删除；目标 Paste 是另一条独立命令。失败不自动撤销源 Cut，但剪贴板可重试。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI04.a — 基础对象不同 TPQN

**具体问题：** 不同 TPQN 项目间粘贴基础音符／事件，最终是否支持默认保留节拍比例、另可选择保留原 Tick 数？例如从 TPQN 192 粘到 480，原 Gate 192 默认变为 480 还是仍为 192；首个切片是否可先限同 TPQN？

**推荐方案：** 首个切片同 TPQN；本批最终支持基础音符／可表达事件的不同 TPQN 粘贴。默认保留节拍比例，可明确改为保留 Tick 数；换算起止边界后只取整一次，按最小 Gate 与碰撞规则提交。

**分歧／代价：** 仅同 TPQN 最省事但限制明显。按节拍转换不保证相同秒数，因为项目 Tempo 可以不同。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：首个切片同 TPQN；本批最终支持基础音符／可表达事件的不同 TPQN 粘贴。默认保留节拍比例，可明确改为保留 Tick 数；换算起止边界后只取整一次，按最小 Gate 与碰撞规则提交。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI04.b — 复杂逻辑依赖不同 TPQN

**具体问题：** 完整事件乐器依赖中包含 Loop、Pre-Roll、Envelope 或表达式内 Tick 常量，无法只按比例无损转换。此类复杂对象本批是否先限制同 TPQN，而裸音符及可转换 Pure MIDI 对象不受此限制，还是必须同时定义复杂依赖的跨 TPQN 转换？

**推荐方案：** 不含复杂逻辑依赖的 Pure MIDI Segment／Track 可按相同原则换算。携带完整 Instrument／SubVoice／Loop／Pre-Roll／Envelope／Mapping 依赖的对象，本批仍限制同 TPQN；裸逻辑音符不因此受限。

**分歧／代价：** 表达式中的 Tick 常量不能普遍无损自动缩放；完整逻辑依赖跨 TPQN 需另定义转换语义，不能只乘比例。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：不含复杂逻辑依赖的 Pure MIDI Segment／Track 可按相同原则换算。携带完整 Instrument／SubVoice／Loop／Pre-Roll／Envelope／Mapping 依赖的对象，本批仍限制同 TPQN；裸逻辑音符不因此受限。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI04.c — 换算舍入与碰撞

**具体问题：** 跨 TPQN 换算得到半 Tick 等非整数边界时，是否在最终起止边界上只进行一次 AwayFromZero 四舍五入，还是需要其他舍入规则？由取整产生的碰撞继续按已确定的音符后来者删除／事件后来者覆盖处理，不另改碰撞语义。

**推荐方案：** 时间换算用精确比例，最终 AwayFromZero 取整，不逐步反复取整；换算后碰撞继续音符后来者删除、事件后来者覆盖。

**分歧／代价：** 如希望另一种舍入规则，在此明确；不要与现有量化中点选早格的另一项合同混为一谈。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：时间换算用精确比例，最终 AwayFromZero 取整，不逐步反复取整；换算后碰撞继续音符后来者删除、事件后来者覆盖。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-MI05.a — 源关闭／崩溃后有效性

**具体问题：** 复制完成后，即使源项目关闭、源实例退出或崩溃，是否仍应能在当前登录会话粘贴？剪贴板随后被替换时，已经开始的目标粘贴是否也应继续完成，而不能依赖源进程一直存活？

**推荐方案：** 剪贴板完整发布后，源项目关闭、源实例正常退出或崩溃，当前登录会话仍能粘贴。剪贴板替换后，已开始的接收任务可继续完成。

**分歧／代价：** 仅源存活可粘贴成本低，但不符合一般预期。手动删除 backing 或磁盘损坏不在有效性保证内。

**用户回答：** 同意，但取决于 D-MI02.d 的讨论结果。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：可靠发布后，源 Project／实例关闭或崩溃不使剪贴板失效，已开始粘贴不因替换中断。Q2 D-MI06.a 确认同目录持久当前槽、原子发布及接收 lease；旧快照仅在非当前且无人使用时回收。

**确认状态：** 已确认；持久槽生命周期见已回答 D-MI06.a。

### D-MI05.b — 重启、历史与长期保存

**具体问题：** 共享剪贴板是否只保证当前登录会话内的正常使用，不保证 Windows 重启、剪贴板历史或云同步中的旧项还能粘贴？还是需要把它扩展为可长期保存的片段／素材库？

**推荐方案：** 不承诺 Windows 重启后、剪贴板历史／云同步中的旧条目仍有效；失效明确报错。这不是永久素材库。

**分歧／代价：** 长期保存应另做显式片段文件／素材库。

**用户回答：** 同意，但取决于 D-MI02.d 中对剪贴板实现的讨论结果，只要目录在，那么即使 Windows 重启也可以继续粘贴。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：完整且兼容的当前文件型剪贴板可跨 Windows 重启继续使用。Q2 D-MI06.a/b 确认只保留单个当前槽、不做历史库，当前项不按年龄删除；同目录升级按独立协议兼容性决定继续使用或明确拒绝，不把目录存在视为内容完整。

**确认状态：** 已确认跨重启、清理和同目录升级边界；不提供历史／云同步素材库。

### D-MI05.c — 跨软件版本协议

**具体问题：** 两份 Midora 的软件版本不同时，只要剪贴板协议兼容是否允许互通，并在不兼容时明确拒绝？还是要求 exe 版本完全相同才能粘贴？

**推荐方案：** 按明确的剪贴板协议版本互通，不要求 exe 版本完全相同；不兼容明确拒绝，不尝试不安全反序列化。

**分歧／代价：** 不承诺任意新旧软件版本互通。

**用户回答：** 不适用。我不打算跨目录跨exe支持剪贴板。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：原跨目录／跨 exe 互通范围仍不适用。Q2 D-MI06.b 单独确认同目录原地升级：校验独立协议／数值契约／能力版本，兼容继续，不兼容明确拒绝并提示重新复制，不自动删除未知文件；不要求任意历史格式迁移。

**确认状态：** 原跨目录范围不适用；同目录升级已在 D-MI06.b 确认。

## 9. 快捷键、Windows 窗口效果与后续动画

关联 R09、R03、R02。R08 图标选择不在本问卷；不因未选图标阻塞这里的行为讨论。

### D-KEY01 — 键位决定方式

**具体问题：** 本批新增／调整功能的快捷键，是由你指定“命令 → 键位”，还是授权实施者先检查冲突、保留既有键位，再为高频新命令选择键位？如果要改旧键位，请在这里明确列出，空白不会被当作授权。

**推荐方案：** 若用户没有指定键位，授权实施者在保留既有快捷键的前提下，为本批高频新增命令选择无冲突键位，并同步菜单提示与使用说明。先做源码冲突检查，不默默改旧键位。

**分歧／代价：** 若有要改的旧键位或明确的新组合，请在回答中给出“命令 → 键位”；原始要求不能推导出唯一键表。此授权本身也待回答，不能因字段空白自行分配。

**用户回答：** 授权。对以前加过的操作也可以加快捷键。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：授权为本批新增以及以前尚无快捷键的操作安排无冲突键位；保留既有键位，不把授权扩成任意重绑定。实施前冻结键表并更新提示。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-KEY02 — 快捷键系统范围

**具体问题：** 本批只需要落实约定好的固定快捷键，还是还要新增用户可自行改键的设置界面、冲突检测、持久化和恢复默认功能？是否要求所有低频菜单项也分配快捷键？

**推荐方案：** 本批不附带可配置快捷键系统，也不要求每个低频菜单项有快捷键。

**分歧／代价：** 若希望用户自行绑定，需批准设置、冲突检测、持久化和恢复默认等范围。

**用户回答：** 同意，本批不附带可配置快捷键系统，也不要求每个低频菜单项有快捷键。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：本批不附带可配置快捷键系统，也不要求每个低频菜单项有快捷键。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-WIN01 — 原生窗口效果与回退

**具体问题：** 原生圆角、阴影或系统动画在某个 Windows 环境不可用时，是否接受回退到当前可靠窗口样式，而不模拟整套原生效果？优先保证 DPI、最大化、拖动和命中正确，还是要求不同系统也尽量保持完全相同的外观？

**推荐方案：** 主窗口和自有弹窗遵循系统能力：Win11 原生圆角／阴影，Win10 方角／阴影，遵守系统动画设置。能力不可用时退回可靠的现有窗口行为，不破坏缩放、最大化、DPI、拖动和命中。

**分歧／代价：** 不为看起来像原生而自行模拟整套系统动画；未实测系统必须明确标记。

**用户回答：** 同意。这不是非做不可的需求。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：尽量采用相应系统原生窗口效果，能力不支持时可靠回退；用户允许暂缓／不实施，不是无条件发布门。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-FX01.a — 动画基调

**具体问题：** 正式发布稳定后再加入动画时，菜单／弹窗应以短淡入、轻微过渡为主，还是希望保留较明显的闪烁等强调效果？若选明显效果，需要在哪些具体位置使用？

**推荐方案：** 发布稳定后，菜单／弹窗等采用短淡入或轻微过渡，不做强烈闪光。

**分歧／代价：** 若希望明显闪烁，需要指定范围；不建议默认。

**用户回答：** 同意。以轻动效为主，不抢用户注意力，且设置里允许彻底关闭动画。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：正式发布稳定后再做轻动效，不抢注意力；Appearance 中提供彻底关闭动画的总开关。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-FX01.b — 亚克力开关

**具体问题：** 亚克力／背景模糊是否做成独立开关并默认关闭，不支持或性能不合格时回退不透明主题？还是希望默认开启并接受不同硬件／系统上的差异？

**推荐方案：** 亚克力独立开关，默认关闭；不支持或性能不合格时用不透明主题。

**分歧／代价：** 默认开启需要接受可用性和硬件差异。

**用户回答：** 同意。这些设置记得放在现有的程序菜单的外观设置Tab里。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：亚克力独立开关、默认关闭，放现有 Appearance；不支持／性能不合格时不透明回退。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

### D-FX01.c — 瓦片动效时机

**具体问题：** 渲染瓦片的出现动画是否只用于新进入区域的首次加载，而编辑、Undo、选择变化和热缓存切换立即更新、不重复动画？还是这些更新也要播放过渡；已有粗概览仍不能因此退回空白。

**推荐方案：** 仅新进入区域的首次就绪使用动效；编辑、Undo、Selection、热缓存复用不反复播放。已有粗概览不退回空白等待动画。

**分歧／代价：** 每次缓存更新都动画会放大闪烁并干扰编辑。

**用户回答：** 同意。这个动效也需要能够在设置里开关，默认开启。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：首次新区域就绪动效独立可开关、默认开启；编辑／Undo／Selection／热复用不反复动画。Q2 D-FX01.e 细化为真正无缓存的等待区域用静态棋盘占位，已有粗概览保留，粗→细替换不播放动效。R02 仍在正式发布稳定后实施。

**确认状态：** 已确认；与已回答 D-FX01.e 合并，不将粗→细当作首次就绪反复动画。

### D-FX01.d — 减少动画与繁忙回退

**具体问题：** 是否提供统一的关闭动效选项、遵守系统减少动画设置，并在繁忙时跳过装饰过渡，优先立即显示已就绪内容？还是有必须保留动画的场景，需要明确列出？

**推荐方案：** 提供关闭动效，遵守系统减少动画设置；繁忙时跳过装饰过渡，不能为播完动画延迟内容出现。

**分歧／代价：** 内容就绪与动画生命周期必须分开。

**用户回答：** 同意。

**补充问答：** 2026-09-14 归纳（非改写用户原答）：提供关闭动效，遵守系统减少动画设置；繁忙时跳过装饰过渡，不能为播完动画延迟内容出现。

**确认状态：** 已确认；以本项用户回答及上述归纳为准，尚未实施。

## 10. 必须安排的技术验证，不是待用户拍板的实现细节

这些是未来实施前／实施中的工程责任，本次未运行产品测试：

| 验证主题     | 必须覆盖                                                                                                                               |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| 乐器变化点   | 导入浏览不改 raw、同 Tick 正式 order、跨 Track 同 Root、pending Bank 与最近 PC 已采用 Bank、部分继承、包装解组、全编辑矩阵、Undo／Redo |
| 试听         | 实际 Master／Limiter、配置冻结、预载、抢占和释放链；当前静态审计风险不是已经证实的可听 BUG                                             |
| Lane／阶梯线 | 多 targets、百万事件、冷页、同 Tick 重复、前驱、隐藏释放、选择一致性；不得常驻另一份全量事件图                                         |
| 恢复         | 音乐优先、坏 section 隔离、资源上限、懒恢复、旧项目读取、迁移／普通 Save／Save Copy 安全                                               |
| 多实例       | 文件所有权、设置竞争、原子剪贴板发布、源崩溃、目标取消、磁盘不足、清理、有限内存、不同安装目录                                         |
| 显示效果     | 连续滚动／键色／动画对比既有基线；必须先测再决定性能门是否通过，不以小样本代表千万级 MIDI                                              |

详见各专题的源码审计与 [00 §6](00-Overview-and-Delivery-Plan.md#6-每轮交付和共同验收门)。具体分页布局、内存／磁盘预算和并发参数先实验、后冻结，不要求用户凭空估算。发现新约束时在本文追加稳定编号问题，而不是在代码里静默退让。

## 11. 环境资料与后续新增问题

### ENV-WIN10 — Win10 实机验收环境（资料，不计入 95 项建议）

**问题：** 是否能提供 Win10 实机验收环境？

**用户回答：** 不能提供“Win10 实机”，但是可以提供虚拟机环境。

**补充问答：** 2026-09-14 已收到环境答复：后续可安排 Win10 虚拟机验证，不将它写成实机／GPU／音频硬件验收；本轮未运行该验证。

**确认状态：** 环境资料已回答；虚拟机测试尚未执行。

### 后续新增问题登记

Q1 95 项和 [§13 Q2 9 项](#13-q2-补充问题2026-09-14) 均已回答，原 32 项没有整项遗漏。Q2 的明确修订已同步 00～05；本轮未发现必须继续 Q3 的产品分歧，不添加空问题。D-FX01.e 仍为正式发布稳定后的后置事项。实施时的性能预算、协议／schema 版本、并发验证由工程调查确定；若实际结果要求改变已确认的工作流／音乐／兼容边界，再以带场景的新 Q3 编号询问，不用旧批准代替新选择。

## 12. 文档维护与交付记录

### 首次问卷落档记录（2026-09-11，保留历史）

| 项目           | 记录                                                                                                                                                                                                                                                                                       |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 输入           | 原 32 项需求文档；随后一次性整理的完整聊天决策清单；“落地到文档，以后文档问答”及“把具体问题放到文档里”的明确请求                                                                                                                                                                           |
| 输出           | Q1 完整问卷、95 项具体问题及必要场景、逐项推荐／回答／讨论／状态栏、统一答复区、状态白名单／所有权表、环境资料栏和后续问答约定                                                                                                                                                             |
| 边界           | 图标选择排除；不替用户批准推荐；不修改音乐代码、SRS、ADR、版本、用户原稿或 dist                                                                                                                                                                                                            |
| 失败／冲突处理 | 空白不算同意；条件不清标讨论中；子项答复优先；不得改写用户原话或用实现默认值替代决定                                                                                                                                                                                                       |
| 持久化归属     | 本次仅仓库 Markdown 开发文档；不是 Project presentation 或 Application Preferences 改动                                                                                                                                                                                                    |
| 验证           | Q1 的95个待决策编号完整且唯一，均已在推荐前补齐具体问题；补充前后的97处用户回答栏逐字符一致，推荐／分歧／确认状态保持不变。此前已校验的17个本目录文件链接、10个目录锚点未改动；已审查问题与建议的一致性，git diff --check通过。产品代码、SRS与用户草稿无差异；未运行产品构建／性能／UI测试 |
| Git／发布      | 本次不提交、推送或本地发布；后续需用户明确要求                                                                                                                                                                                                                                             |

### 问卷变更记录

| 修订        | 日期       | 变化                                                                                                                       | 决定状态                     |
| ----------- | ---------- | -------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| Q1          | 2026-09-11 | 聊天完整版落档；95 项建议全部提供回答／讨论／状态栏；统一原推荐更正；默认文档问答                                          | 尚未回答／定案               |
| Q1 表达补全 | 2026-09-11 | 为原 95 项逐一补充明确问题及必要场景，不新增决策项、不改变编号或推荐；保留用户已编辑的回答栏。后续问答也必须先写清具体问题 | 推荐与确认状态不变，仍待回答 |
| Q1 回答归并 | 2026-09-14 | 95 项均已回答；逐项保留原答和 Q1 推荐，写入当前归纳；同步 00～03；新增 05 汇总                                             | 已确认部分／继续讨论分开     |
| Q2          | 2026-09-14 | 新增 9 项具体边界问题；未加载反馈为后置事项；不含新增图标选择题                                                            | 提问时全部待确认，现已由下一条归并 |
| Q2 回答归并 | 2026-09-14 | 先提交推送 `7227090`；9 项原答全保留，Draw 修正、连续范围扩展等同步 00～05；104 项完整复核 | 当前均已收敛，未发现必须 Q3 |

### Q1 归并轮边界（2026-09-14，保留历史）

- 输入：已回答的 Q1、原 32 项需求及相关源码／现行规格。
- 输出：原答不变的确认台账、9 项明确补问、专题同步及覆盖检查；只写本任务 Markdown。
- Git：先提交、推送 `3619dee`；其后的归并改动不再次自动提交／推送。
- 未做：产品实现、SRS／ADR／版本修改、构建、性能实验、UI 操作或本地发布。

## 13. Q2 补充问题（2026-09-14）

以下 9 项均已回答，当前结论见各项补充问答／状态。原问题、原推荐和原答全部保留，特别是 D-UI01.d 和 D-VIS02.f 的原问题前提已被用户明确修订，不得按历史前提实施。无需重答 Q1／Q2，也不重选已指定图标。

<a id="q2-d-ui01-d"></a>

### D-UI01.d — 三个 Pen 工具在非数值区如何工作

**关联：** D-UI01.a；R18。**最迟确认时机：** A3 手势实施前。

**具体问题：** 你已确定删除 Draw、提供三个独立 Pen。选中 Line／Horizontal 后，去上方音符区、Arrangement Segment 区或 Inst. 点区操作，应该如何工作？是只有可画数值线的区域提供三种 Pen，其他区域只保留 Free；还是所有区域都保留三种按钮，但在非数值区等同普通创建／移动？

**推荐方案：** 只在支持数值绘线的事件／参数／Velocity／Tempo 区提供三种 Pen；音符、Segment 与 Inst. 区用 Free 承担原 Draw 行为，不新增“沿线画音符”。D 选择当前焦点区域的 Free。主音符区和下部绘线区分开记录工具选择；是否在同轨道间共享另见 D-STATE03.e。

**分歧／代价：** 当前主 Piano 与底部 Lane 存在共享 ToolMode 的绑定，不能只换按钮而不定义行为。若希望所有区域均显示三种工具，也可指定其退化为 Free 的规则；不会把已指定图标再交回给你重选。

**用户回答：**

这是我的错误判断，我误认为目前事件区域和钢琴区域的Tool是分离的，但其实是共享的。

撤回“删除 Draw、提供三个独立 Pen”的描述，而是继续保留 Draw 工具，然后在事件视图额外添加一个工具组，里面有三个工具按钮（纯图标按钮），分别对应Free、Line、Horizontal；在非 Draw 模式下，禁用这三个按钮。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：用户明确修订并撤回 Q1“删除 Draw”及本题推荐中按区域拆主工具的前提。保留原共享 ToolMode／Draw；适用事件／参数／Velocity／Tempo 数值区另加 Free／Line／Horizontal 纯图标形态组，非 Draw 模式禁用。D 仍进入 Draw，不因按 D 默默重置已选绘线形态；音符／Segment 不增加沿线创建。D-STATE03.e 所引用的最终区域划分据此为主工具＋绘线形态两个字段，不是两套 Draw／Select。

**确认状态：** 已按用户明确修订确认；覆盖冲突的 D-UI01.a 历史原答和本题原推荐，尚未实施。

<a id="q2-d-lane01-g"></a>

### D-LANE01.g — 隐藏 Lane 后的全部目标目录

**关联：** D-LANE01.a/b；R28。**最迟确认时机：** A2b Lane Tabs 实施前。

**具体问题：** 是否采用“普通 Lane Tab 可隐藏；最右侧下箭头打开包含已显示与已隐藏 target 的目录，列出事件／参数名称、精确数量及显隐，点击即可显示并激活”？无点但有曲线或 Mapping owner 的目标也列出并标注；Vel.／Inst. 仍按你的决定常驻。还是需要独立的新窗口／面板来查这些内容？

**推荐方案：** 复用已计划的最右下箭头为可搜索、虚拟化的全部目标目录，不另加常驻面板。计数使用 owner＋target 的派生摘要，随导入／编辑／Undo 更新，打开目录不重新扫描全部事件；旧数据缺摘要时后台补建，期间显示“统计中”。

**分歧／代价：** 当前源码的目标发现仍有全范围扫描，实施时必须一起改造，不能声称现有实现已无 O(N)。常规计数查询可按 target 数 K 工作；首次建立摘要仍有实际成本。关闭 Tab 永不删除正式数据／owner。

**用户回答：** 同意。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：同意本题推荐：普通 target Tab 可隐藏；下箭头目录包含已显示／隐藏项，提供名称、精确数量、显隐、搜索与虚拟化，点击显示并激活。无点但有曲线／Mapping owner 仍列出并标明，Vel.／适用 Inst. 常驻。owner＋target 摘要随导入、编辑、碰撞、Undo／Redo 更新；缺摘要一次后台有界可取消补建，期间显示统计中，不报假 0，不重复 O(N) 扫描。

**确认状态：** 已确认；实现与冷／热性能验证留 A2b，不需继续问显隐方案。

<a id="q2-d-val01-d"></a>

### D-VAL01.d — 显示值表达式是否扩展到 Project Mapping 链

**关联：** D-VAL01.a/b/c；R12。**最迟确认时机：** A4 数值契约实施前。

**具体问题：** Q1 已确定编辑表达式用显示值。这里是否仅指 Batch Edit／Batch Create 等编辑工具，还是连项目内 Mapping Function 与整条 Mapping 链也要一起改？例如 CC10 的 raw96 显示32，Batch 的 =p0*0.5 将得到显示16（raw80）；现有 Mapping 的 value*0.5 或内置 Multiply 0.5 则按 raw 得到48（显示−16）。你希望这两类用途保留各自契约，还是 Mapping 也统一按显示32计算？

**推荐方案：** 限定到编辑显示和数值编辑工具：目标 CC 的属性／坐标／列表／工具输入输出使用显示域，最终编码回 MIDI；Project Mapping ABI、共享 accumulator 和既有音乐语义先不变。这符合原 R12“底层值不变、UI 增加映射”及 Q1 具体点名 Batch Edit 的范围。若你也要改变 Mapping，则为该目标整条链另定一致的显示域契约，不只在函数入口偷偷减64。

**分歧／代价：** 扩大到 Mapping 会改变可听结果，涉及内置 Step、Function、TargetOriginalValue、Initial State／原始值、最终取整与 ABI；Logical Parameter 自有值域、乘法因子和增量不能机械减64。你已批准不兼容旧工具表达式／预设，不等于放弃所有旧 .midora 音乐读取；两者必须区分。

**用户回答：** 同意，限定到编辑显示和数值编辑工具。区分“外侧编辑层”和“数据层”。当然，Help 里，要写清楚这个区别。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：用户明确限定为外侧编辑层，且要求 Help 解释与数据层的区别。CC10、71～78 的显示／编辑工具使用 −64..63，最后转 raw 0..127；Project Mapping ABI、内置 Step、累计链及原始音乐语义不改。Help 对照 raw96／显示32：Batch *0.5 得显示16／raw80，Mapping *0.5 仍得 raw48／显示−16；仅旧编辑工具表达式／预设结果兼容可破坏。

**确认状态：** 已确认边界及 Help 要求；不扩大到 Mapping 音乐契约。

<a id="q2-d-vis02-f"></a>

### D-VIS02.f — 全局连续跟随遇到其他视图

**关联：** D-VIS02.a；R10。**最迟确认时机：** 连续跟随实施前。

**具体问题：** 顶部 Follow 已改为全局三态，但本批连续滚动只要求两类 Segment 与 All Tracks。保持“连续”后切到 Arrangement／Conductor 等原本支持跟随、却未纳入连续改造的视图时，应沿用它们的跳跃跟随，还是暂不跟随？

**推荐方案：** 保留全局连续选择；支持连续的视图连续滚动，其余原本支持跟随的视图使用原有跳跃行为，并在按钮 Tooltip 说明当前视图实际采用的方式。切回无需重新选择。不向原本无跟随的视图新增能力。

**分歧／代价：** 也可让非适用视图暂停，但按钮亮着而不跟随较容易困惑；把连续扩大到所有视图是新增范围，不能默默实施。

**用户回答：**

这是我的需求描述缺漏。我想让 Arrangement／Conductor 也支持连续滚动跟随。

不过，如果其他仍然有支持跟随的视图却没有被本次（两类 Segment、All Tracks、Arrangement、Conductor）纳入的，或未来添加的视图要支持跟随，在实现连续滚动前，先按照“支持连续的视图连续滚动，其余原本支持跟随的视图使用原有跳跃行为”为准。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：用户明确新增 Arrangement／Conductor；本批连续跟随覆盖两类 Segment、All Tracks、Arrangement、Conductor。其他已有或未来 Follow 视图，在尚未实现连续前保留全局选择并使用原跳跃行为，Tooltip 说明实际方式，切回支持视图无需重选；原本无 Follow 不因此新增。新增两视图仍纳入冷／热性能门，不把 R11 琴键染色也扩到它们或 SubVoice。

**确认状态：** 已按用户扩展范围确认；不是接受本题原范围不变的前提。

<a id="q2-d-state03-d"></a>

### D-STATE03.d — 删除来源后其他视图的 Onion 引用

**关联：** D-STATE03.c；R06/07。**最迟确认时机：** B1 状态所有权实施前。

**具体问题：** 已确定删除对象后立即释放它自身的视图状态。若轨道 A 仍存在、其 Onion 手选来源含轨道 B，此时删除 B：是否仍在 A 的轻量配置里保留 B 的失效身份引用，以便音乐 Undo 恢复 B 后重新显示；还是连 A 中这条引用也立即删除、Undo 后需手动重选？

**推荐方案：** 删除 B 自身的 profile、Tab／viewport 状态和缓存；A 保留一条受条目预算约束的 dormant 来源身份，沿用现有 Onion 的 Undo 体验。该身份不持有 B 的对象、数据页、位图、任务或资源 lease，保存仍过滤悬空引用、关闭项目释放。

**分歧／代价：** 当前 SRS §18.11.3／INV-116 已规定 Onion dormant＋Undo，与你否决“已删 owner 自身状态保留”不是必然同一对象。若希望所有失效引用也立即清除，可以修改，但需明确接受 Undo 不恢复 A 的来源选择。

**用户回答：** 同意。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：同意本题推荐：删除 B 立即释放 B 自身 profile、Tab／viewport、缓存等资源；存活 A 只保留受预算的 dormant B 来源 ID，音乐 Undo 恢复 B 后可再显示。不保留 B 对象、数据页、位图、任务或 lease；保存过滤悬空引用、关项目释放，不自动重开 B 的 Tab。

**确认状态：** 已确认；与禁止保留已删除 owner 自身视图状态不冲突。

<a id="q2-d-state03-e"></a>

### D-STATE03.e — 当前工具的同轨道共享范围

**关联：** D-STATE01.a、D-STATE03.a；D-UI01.d。**最迟确认时机：** B1 状态所有权实施前。

**具体问题：** 保存白名单包含“当前工具”，但同 Track 共享表此前没有单列它。同一 Track 打开两个 Segment 时，在一个视图切换 Free／Select／Erase 或事件的 Line／Horizontal，另一个 Segment 是否应同步工具，还是各 Segment 记自己的工具？

**推荐方案：** 将当前工具纳入 Track 通用 profile；按 D-UI01.d 最终区域划分分别保存主区与事件区工具，同 Track 同区域同步、不同 Track 隔离。SubVoice 各自独立，Arrangement／Conductor 保留各自工具；对不支持的工具使用明确合法默认值。

**分歧／代价：** 各 Segment 独立也可行，但会出现缩放／Snap 同步、工具不同的体验。此问题只补工具字段所有权，不重新开放已批准的整张状态白名单。

**用户回答：** 同意。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：同意将当前工具纳入 Track 通用 profile，并依本题引用的 D-UI01.d 最终修订解释：主钢琴卷帘与底部 Lane 保留共享主 ToolMode，另记事件绘线形态；这两个字段均同 Track 同步、不同 Track 隔离，不建立两套主 ToolMode。SubVoice 各自独立，Arrangement／Conductor 各自保存，恢复不支持的值时合法回退。

**确认状态：** 已确认；以 D-UI01.d 的最终主工具／子形态定义落实附表 B。

<a id="q2-d-mi06-a"></a>

### D-MI06.a — 文件型剪贴板的当前槽与清理

**关联：** D-MI02.d、D-MI05.a/b；R01。**最迟确认时机：** C1 存储协议实施前。

**具体问题：** 按你的同目录文件型剪贴板方案，是否只保留最近一次成功 Midora Copy／Cut，并在重启后继续使用？在别的软件复制文字是否不影响这个音乐剪贴板；下次 Midora Copy／Cut 或显式 Clear Clipboard 才替换／清除？还是你希望历史内容也长期保留？

**推荐方案：** 一个持久当前槽，不做历史库。建议完整快照和小型 current 索引放在 `<ProgramRoot>\Data\SharedClipboard`，未完成准备放在 `<ProgramRoot>\.tmp\SharedClipboard`；仅同 ProgramRoot、同用户实例共享。音乐视图 Paste 读此槽；文本框仍使用系统文本剪贴板。替换采用原子发布，旧快照只有在不再为当前项且接收 lease 全释放后回收；失败 Copy 不改变原当前项。

**分歧／代价：** 要跨重启，仅目录存在不够，还需完整、兼容的已发布内容和可定位当前项的索引；不能随源退出清理。当前槽不按年龄自动删除，提供显式清除及资源预算；若磁盘不足，保留旧项并让新 Copy／Cut 安全失败。路径白名单、锁、崩溃回收和安全校验须纳入后续设计，不写到用户目录／任意外部路径。

**用户回答：** 同意。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：同意单个持久当前槽：完整快照＋current 索引置于 `<ProgramRoot>\Data\SharedClipboard`，未完成准备置于 `<ProgramRoot>\.tmp\SharedClipboard`；仅同目录、同用户共享。外部复制文字不覆盖音乐槽，文本框仍用系统剪贴板。Copy／Cut 原子发布成功才替换，发布失败保旧槽；Cut 在可靠发布后才删除源，源删除失败保持源数据，不反向撤销已发布的跨实例槽。旧非当前快照待全部接收 lease 释放才回收，源退出／重启不使当前槽失效；不按年龄删当前项，提供显式 Clear 和预算，准备／发布时磁盘不足安全失败。正式路径白名单和实现验证待 C1 落实。

**确认状态：** 已确认存储位置、独立文本行为及生命周期；不做历史库，不在内存或其他目录偷偷兜底。

<a id="q2-d-mi06-b"></a>

### D-MI06.b — 同目录升级后如何处理旧剪贴板

**关联：** D-MI05.b/c；R01。**最迟确认时机：** C1 文件协议实施前。

**具体问题：** 虽然不支持跨目录／跨 exe 互通，但原地替换同一目录的 Midora.exe 后，磁盘仍可能留下上一版本的剪贴板。应允许明确兼容的旧文件继续粘贴、不兼容时清楚提示，还是每次软件版本变化就令旧剪贴板失效？

**推荐方案：** 文件带独立协议／数值契约／能力版本和严格边界校验；兼容则继续使用，不兼容明确拒绝并提示重新复制，不自动删除未知文件。这不增加跨目录共享，也不要求任意历史版本迁移。

**分歧／代价：** 不能因为同路径就用新布局硬读旧字节；程序版本与文件协议不同。若选择“升级即失效”，则跨重启可用承诺需明确排除软件版本改变的情况。

**用户回答：** 同意。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：同意独立协议／数值契约／能力版本与严格校验；同目录升级后兼容则继续粘贴，不兼容明确拒绝、提示重新复制，不自动删除未知文件。软件版本改变不必然失效，不扩大跨目录／跨 exe 范围、不承诺任意历史版本迁移。

**确认状态：** 已确认；协议版本号及编解码／安全验证为实施任务，不是新产品决策。

<a id="q2-d-fx01-e"></a>

### D-FX01.e — 未加载完成区域的反馈

**关联：** D-FX01.c/d；R02。**最迟确认时机：** 仅正式发布稳定后的动效阶段，非近期阻塞。

**具体问题：** 原始 R02 除了完成瞬间动效，还举过未加载时闪烁提示。对于尚无缓存的区域，应仅显示低强调静态“加载中”提示，还是让该提示轻微闪烁／呼吸？已有粗概览时是否不加遮罩，直接保留内容等待细化？

**推荐方案：** 已有内容始终保留；真正尚无内容时只显示轻量静态提示，不让大面积画布闪烁。首次完成动效继续按已批准的独立开关执行；总关闭／系统减少动画优先。

**分歧／代价：** 若要呼吸效果，只作用于小提示而非全画布，并受 Appearance 总动效开关和繁忙回退控制。此处先定体验，性能参数届时实测；不提前实施后置需求。

**用户回答：** 同意。尚无缓存时静态展示占位图（明显与对象内容有颜色区分的棋盘），粗概览->细概览的过程不需要任何动效。

**补充问答：** 2026-09-14 Q2 归并（保留用户原答）：用户明确选择无缓存等待区的静态棋盘占位，颜色明显区别于对象内容；不闪烁／呼吸。已有粗概览始终保留，粗→细无动效；首次空白→就绪仍遵守 D-FX01.c/d 的独立开关、默认开及总关闭／减少动画优先。已确认空内容与仍在等待必须区别，不能把正常空白永久画成加载中。此项仍仅在正式发布稳定后实施。

**确认状态：** 已按用户细化确认；不是近期动效实施授权。

## 14. Q2 归并收尾与后续进入条件

- **输入／输出：** 用户全部 Q2 原答、Q1 及原 32 项；输出为同目录决策台账与专题同步，不是产品改动。
- **Git 顺序：** 已先提交并推送 `7227090`；本轮后续整理留工作区，未再次提交／推送。
- **查漏结论：** Q1 95 项、Q2 9 项均有答复；图标准备不在问卷内。未发现必须 Q3 的遗漏或冲突，具体问题／推荐／分歧和用户原话不改写。
- **工程待办：** 完整手势／焦点回归、目录摘要增量与冷构建、Mapping 不变性、各视图 Follow 性能、presentation 预算及故障隔离、跨进程协议／锁／lease／原子发布。这些尚未实现或运行验证，不把它们伪装成缺少用户决定。
- **实施授权：** 仍需用户明确选择开始实施；按 00 的优先级与建议切片推进，不因本轮收敛自动启动代码或规格修改。R02 后置、R08 待图标准备、R10／R11 先测性能的条件保留。
- **后续提问：** 只有工程调查出现影响声音、文件兼容性或用户工作流的新选择时才新增具体 Q3；不为页长、缓存预算等可测试事项要求用户猜数值。
- **验证边界：** 本轮仅文档一致性、原答保护及静态源码交叉复核；没有产品构建、性能／UI／音频测试、SRS／ADR／版本改动或本地发布。
